# Chaptify — прогресс интеграции

Статусный документ по итерациям разработки сайта chaptify.ru. Обновляется в конце каждой итерации.

---

## Итерация 1 — главная страница + каркас

### Сделано
- `SiteHeader` (server component) с навигацией, поиском, кнопкой профиля/входа
- `SiteFooter` (минимальный)
- `layout.tsx`: Inter + Lora через `next/font/google` → `--font-sans` / `--font-serif`
- `WeeklyHero` с живыми данными (новые главы за неделю, свежее обновление)
- `GenreChips` с агрегацией жанров из `novels.genres` jsonb
- `ContinueReadingShelf` + `MyShelfStrip` для залогиненного пользователя (из `profiles.last_read` / `bookmarks`)
- CSS для полок (`.shelf-scroll`, `.continue-card`, `.shelf-strip`, `.shelf-thumb`)

### SQL миграция 001 — `migrations/001_roles_coins_subscriptions.sql`
Накатывать **вручную** на Supabase. Безопасно для tene.fun.
- `profiles.role`, `translator_slug`, `coin_balance`, прочие колонки переводчика
- `novels.translator_id` + обратная совместимость (все старые новеллы получают Алёну)
- Таблицы: `subscriptions`, `coin_transactions`, `chapter_purchases`
- RPC: `add_coins`, `buy_chapter`, `can_read_chapter`, `get_user_subscription_status`

---

## Итерация 2 — каталог + 3 киллер-фичи

### Сделано
- `/catalog` — серверные фильтры через URL query params (SSR, shareable-ссылки)
- Сайдбар с 5 группами фильтров: Настроение · Время чтения · Статус · Жанр · Сортировка
- Пагинация с эллипсисом, 24 на страницу
- Пустое состояние

### Киллер-фичи каталога

1. **«Настроение»** — `src/components/MoodPicker.tsx`, `src/lib/catalog.ts::MOODS`
   6 карточек: 🥺 Поплакать · 🍵 Уютно · ⚔️ Адреналин · 😄 Посмеяться · 🧠 Подумать · 💕 Любовь. Каждое настроение — набор жанров + минимальный рейтинг.

2. **«Время чтения»** — бейдж на карточке (~8 ч, ~2 дн.), фильтр-баскеты Быстро/На вечер/Марафон/Эпос. Формула: `chapter_count × 15 мин`.

3. **«Забытое на полпути»** — `src/components/ForgottenNovels.tsx`. Персональная секция на главной: новеллы, начатые 14+ дней назад и с прогрессом <90%. Кнопка «Продолжить».

### SQL миграция 002 — `migrations/002_catalog_extensions.sql`
- `novels_view` пересоздан с колонками `chapter_count`, `last_chapter_at`, `translator_id`
- Старые колонки view сохранены → tene не сломается
- GIN-индекс на `novels.genres`, индексы на `chapters.novel_id` и `chapters.published_at`

---

## Итерация 3 — страница новеллы + читалка + 3+3 киллер-фичи

### Страница новеллы `/novel/[id]` — новое
- `FirstChapterPreview` — карточка с первым абзацем первой главы и кнопкой «Дочитать» *(киллер #1)*
- `SimilarByReaders` — рекомендации через RPC `get_similar_novels_by_readers` *(киллер #2)*
- `ReleasePace` — столбчатая диаграмма выхода глав за 90 дней + прогноз темпа/дня недели *(киллер #3)*
- Бейдж «~8 ч» рядом с жанровыми note
- Fallback «Похожее от автора» если коллаборативка пустая

### Читалка `/novel/[id]/[chapterNum]`
Перенесли из tene `ChapterReader.jsx`:
- Шрифты: Inter, Lora, Merriweather, Roboto, OpenDyslexic
- Размер шрифта, высота строки, выравнивание, красная строка, отступ абзацев
- Настройки сохраняются в `localStorage` (`chaptify-reader-settings`)
- Прогресс чтения: `localStorage` + `profiles.last_read` (paragraphIndex + chapterId)
- Автовосстановление позиции при повторном заходе
- Горячие клавиши: +/−, F (фокус)
- Буквица на первом абзаце главы

### Киллер-фичи читалки

1. **Фокус-режим** (`src/components/ReaderSettings.tsx` + CSS в `ReaderContent.tsx`)
   Затемняет все абзацы кроме текущего (тот, что ближе к середине вьюпорта). Scroll → смещается активный абзац. Переключается горячей клавишей F.

2. **Умные закладки с цитатами** (`src/components/QuoteBubble.tsx`)
   Выделил текст мышью/тапом → появляется всплывающая кнопка «⊹ Сохранить цитату». Сохраняет в `user_quotes` (таблица с RLS). Коллекция цитат лежит в профиле — можно перечитывать любимое без перечитывания главы.

3. **Таймер сна** (`src/components/SleepTimerOverlay.tsx`)
   Пресеты 15/30/45/60 мин в настройках. По истечении — мягкий overlay «Пора сделать паузу» с кнопками «Ещё 15 минут» или «Спокойной ночи».

### SQL миграция 003 — `migrations/003_quotes_recommendations.sql`
- Таблица `user_quotes` (RLS включён, select/insert/delete только своё)
- RPC `get_similar_novels_by_readers(p_novel_id, p_limit)` — коллаборативная фильтрация
- RPC `get_release_pace(p_novel_id, p_days)` — темп выхода по дням

---

## Итерация 4 — админка: добавление новелл и глав + 3 киллер-фичи для переводчика

### Сделано

**Шапка (SiteHeader)** — кнопки по ролям:
- Гость: `Войти`
- `user`: `Стать переводчиком` + `Профиль`
- `translator`/`admin`: `+ Новелла`, `Админка`, `Профиль`

**Middleware** — упрощена логика беты:
- `/translator/apply` доступна любому залогиненному
- Остальное — только translator/admin; не-переводчики → `/translator/apply` (раньше был `/beta`, теперь отдельная страница не нужна)

### Новые страницы

- `/translator/apply` — форма заявки. Поля: мотивация (20–2000 симв), портфолио, языки (KR/CN/JP/EN), желаемый slug. Показывает статус существующей заявки («pending», «rejected» с комментарием модератора).
- `/admin` — дашборд:
  - Список «Мои новеллы» (для translator) или «Все новеллы» (для admin). Каждая строка: обложка, название, счётчики, статус модерации, кнопки `+ Глава` и `Редактировать`.
  - Секция «Заявки в переводчики» (видна админу): мотивация, slug, языки, кнопки «Одобрить»/«Отклонить» → вызывают RPC `approve_translator_application` / `reject_translator_application`.
- `/admin/novels/new` — создание новеллы.
- `/admin/novels/[id]/edit` — редактирование новеллы + глоссарий на той же странице.
- `/admin/novels/[id]/chapters/new` — создание главы.
- `/admin/novels/[id]/chapters/[chapterNum]/edit` — редактирование главы.

### Страница новеллы — владельцу показываются кнопки
- `+ Добавить главу`, `Редактировать` рядом с «Читать»
- `Править` на каждой строке главы в списке

### Киллер-фичи для переводчика

1. **Глоссарий проекта** — `src/components/admin/GlossaryPanel.tsx` + таблица `novel_glossaries`
   Словарь `термин оригинала → перевод` с категорией (персонаж/место/термин/техника) и примечанием. В форме главы:
   - Сайдбар со списком первых 10 терминов
   - В live-preview pane каждое совпадение с глоссарием подсвечено `<mark class="glossary-match">` (жёлтая подложка), на hover — перевод в title
   - Счётчик «Совпадений с глоссарием: N» в заголовке preview
   
   В будущем: читатель в главе сможет нажать на подсвеченный термин и увидеть объяснение.

2. **Автосохранение черновика** — `src/components/admin/ChapterForm.tsx` + `src/components/admin/DraftBanner.tsx` + таблица `chapter_drafts`
   При изменении content или chapter_number → debounced 2 сек → upsert в `chapter_drafts` (UNIQUE user_id+novel_id+chapter_number). Индикатор «✓ Черновик сохранён». При открытии формы, если для этой новеллы есть черновик — показывается `DraftBanner` с кнопками «Восстановить» / «Начать заново».

3. **Статистика главы в реальном времени** — `src/components/admin/ChapterStats.tsx` + `computeChapterStats()` в `src/lib/admin.ts`
   Сайдбар, обновляется при каждом изменении текста:
   - Слова, знаки, абзацы, расчётное время чтения (180 wpm)
   - Топ-6 повторов (исключая стоп-слова из ~100 русских/английских)
   - Длинные предложения (30+ слов) — с алёртом «Возможно стоит разбить»

### Компоненты админки
- `CoverUpload` — drag & drop загрузка в Supabase Storage bucket `covers`, лимит 5 МБ, превью
- `NovelForm` — все поля: названия на 3-х языках, автор, страна (KR/CN/JP/Other), age rating, год, статус перевода, флажок «оригинал завершён», жанры (предопределённые + свои), описание
- `ChapterForm` — split-view редактор (textarea слева, preview справа), toolbar (¶/B/I/H/❝), номер главы, флажок «Платная»
- `AdminApplications` — UI для админа над таблицей заявок с prompt-диалогами для комментариев
- `DraftBanner` — карточка с офером восстановить черновик

### SQL миграция 004 — `migrations/004_moderation_and_drafts.sql`

Новые колонки в `novels`:
- `moderation_status` ENUM draft/pending/published/rejected
- `title_original`, `title_en`, `alt_titles` jsonb
- `country`, `age_rating`, `translation_status`, `release_year`
- `external_links` jsonb, `rejection_reason` text

Новые таблицы:
- `translator_applications` — заявки с RLS (self-read/insert, admin-all), партиальный UNIQUE index для одной `pending` на пользователя
- `novel_glossaries` — с RLS: читают все, пишут только владелец новеллы или админ
- `chapter_drafts` — с RLS: self-all, UNIQUE (user_id, novel_id, chapter_number)

Новые RPC:
- `approve_translator_application(id, note)` — выставляет `role='translator'` + копирует `desired_slug`
- `reject_translator_application(id, note)`

### Заметки из ranobelib.me (для будущих итераций)

Что понравилось и может пригодиться позже:
- Фон новеллы (баннер поверх обложки) — не делаем сейчас, добавим, когда понадобится визуальный акцент
- Поля «Издатель», «Художник», «Команда озвучки», «Франшиза» — манга-специфика, не нужны для новелл
- Разделение «Статус тайтла» (оригинал) и «Статус перевода» — у нас `is_completed` = статус оригинала, `translation_status` = наш; сделано
- «Загрузка глав: Все / Создатель и переводчики» — ACL на добавление, стоит добавить, когда появятся «команды» переводчиков
- TipTap-редактор для описания — пока textarea с HTML, позже можно заменить на простой contenteditable с toolbar
- «Ссылки на оригинал и анлейт» — колонка `external_links jsonb` уже есть, UI в форме не добавлен
- Sticky footer с алертом «отправится на модерацию» — сделано в .admin-form-footer
- Checkboxes «Маркировка» (ненорм. лексика, наркотики) — можно добавить в будущем, колонок под это нет

## Итерация 5 — фикс редиректа + профиль + 3 киллер-фичи

### Баг-фикс: /admin редиректил на /translator/apply
Причина: миграция 001 (колонка `role`) ещё не накачена на проде, у пользователя только legacy `is_admin = true`. Middleware смотрел только на `role`, поэтому считал всех «обычными пользователями».

Починено везде, где была проверка роли:
- `src/middleware.ts` — фоллбэк `is_admin === true → доступ`
- `src/components/SiteHeader.tsx`
- `src/app/admin/page.tsx`
- `src/app/admin/novels/[id]/edit/page.tsx`
- `src/app/admin/novels/[id]/chapters/new/page.tsx`
- `src/app/admin/novels/[id]/chapters/[chapterNum]/edit/page.tsx`
- `src/app/novel/[id]/page.tsx` (`canEdit`)

Теперь работает и БЕЗ миграций (на legacy-схеме tene), и С ними (после 001). SELECT `*` делает запрос толерантным к отсутствию колонок.

### Профиль `/profile` — переписан на реальные данные
Раньше была статичная заглушка. Теперь:
- Шапка: аватар (первая буква), имя (`translator_display_name` / `user_name` / email), badge роли («Админ» / «Переводчик»), кнопка «Админка» для переводчиков
- Три stat-карточки: баланс монет, закладки, сохранённые цитаты
- Грид «Книжная диета» + «Читаю сейчас»
- Большой блок «Читательский стрик»
- Блок «Мои цитаты» внизу

### Киллер-фичи профиля

1. **Моя коллекция цитат** (`src/components/profile/QuoteCollection.tsx`)
   Читает `user_quotes` + джойнит названия новелл. Группирует по новеллам, на каждую показывает до 3 цитат + кнопка «ещё N». На цитате: ссылка на главу («→ глава 17»), время («3 дн. назад»), кнопка удалить.

2. **Читательский стрик** (`src/components/profile/ReadingStreak.tsx` + RPC `get_reading_activity`)
   - Три счётчика: текущий стрик (дни подряд, включая сегодня/вчера), рекорд, активных дней за 90
   - Calendar heatmap 13×7 (как у GitHub), 5 уровней яркости по количеству глав в день
   - Fallback: если RPC нет (миграция 005 не накачена), считает активность из `profiles.last_read`
   - Эмоджи 🔥 появляется при стрике ≥ 3 дней

3. **Книжная диета** (`src/components/profile/BookDiet.tsx`)
   - Анализирует жанры прочитанных новелл (из `last_read` → `novels_view`)
   - Инсайт в стиле «Ты любишь **Фэнтези** — 67% твоих новелл»
   - Bars-чарт по жанрам, разбивка по странам (KR/CN/JP/Other)
   - Блок «Попробуй ещё» — 3 новеллы из непокрытых жанров, отсортированные по рейтингу

### SQL миграция 005 — `migrations/005_reading_days.sql`
- Таблица `reading_days (user_id, day, chapters_read)` с RLS (self-read)
- RPC `log_reading_day()` — инкрементирует счётчик за сегодня; вызывается из `ReaderContent.saveProgress` (не блокирует UI, игнорирует ошибки)
- RPC `get_reading_activity(p_user, p_days)` — возвращает N дней активности с нулями
- Бэкфилл: `backfill_reading_days_from_last_read()` (вызывается один раз в миграции) — сеет дни из `profiles.last_read.timestamp`

## Итерация 6 — лента, страница переводчика, поиск (+ 3×3 киллеров)

### Новые страницы

**`/feed` — лента обновлений** (`src/app/feed/page.tsx`)
Хронологическая лента всех свежих глав со всех новелл. По дефолту — все, в каждой строке: обложка, название новеллы, номер главы, время, ссылка на переводчика, индикатор «платная».

Киллер-фичи:
1. **«Только моя полка»** (`src/components/feed/FeedFilter.tsx`) — фильтр через `?mine=1`, ограничивает по `profiles.bookmarks`. Персональная лента без шума.
2. **Детектор «горячих серий»** — подсчёт глав по новелле за последние 3 дня; если ≥ 3 — бейдж 🔥 в карточке + фильтр `?hot=1`.
3. **Отметка «★ в закладках»** — в режиме «всё» подсвечивает те строки, которые из полки. Легко сканировать.

**`/t/[slug]` — страница переводчика** (`src/app/t/[slug]/page.tsx`)
Лукапит профиль сначала по `translator_slug`, потом fallback по `user_name` (для legacy tene). Новеллы — сперва по `translator_id`, fallback по `author ILIKE user_name`. Шапка с аватаром, bio, счётчиками + боковой блок «Поддержать» (Boosty / Tribute / Купить монеты).

Киллер-фичи:
1. **«Рукопожатие»** (`src/components/translator/TranslatorHandshake.tsx`) — считает, сколько новелл этого переводчика visitor уже читал (`last_read` + `bookmarks`). Показывает «Вы совпали на 3 новеллах · 60% от его каталога» + названия первых 3-х. Если самопосещение — баннер «Это твоя страница, перейди в админку».
2. **Heatmap выпусков за 6 месяцев** (`src/components/translator/ReleaseHeatmap.tsx`) — 26×7 grid, 5 уровней яркости по количеству глав в день. Видно, активный переводчик или в паузе.
3. **Специализация** (`src/components/translator/TranslatorSpecialty.tsx`) — breakdown жанров с инсайтом «В основном ромфэнтези: 67%» + секция «Топ-3 в жанре "ромфэнтези"» с миниатюрными карточками.

**`/search` — поиск** (`src/app/search/page.tsx` + `src/components/search/SearchClient.tsx`)
Клиентский инстант-поиск с debounce 260 мс. Запрос к `novels_view` через `OR` по title/title_en/title_original/author/description + параллельный запрос к `novel_glossaries`.

Киллер-фичи:
1. **Инстант-поиск** — результаты во время набора, без кнопки «Найти». URL синхронизируется через `replace` (скроллинг не сбрасывается).
2. **Поиск по глоссариям** — отдельная секция «Термины и персонажи»: ищешь «Хёнджэ» или «старший брат» → находишь новеллы, где этот термин в глоссарии. Показывает категорию (персонаж / термин / техника).
3. **«Возможно, вы хотели X?»** — если 0 результатов, считает Левенштейна между запросом и популярными жанрами, предлагает ближайший (например, «романтика» вместо «романика»).

### Прочие изменения
- `SiteHeader` — инпут поиска теперь в `<form action="/search">` → Enter ведёт на `/search?q=...`
- Страница новеллы — блок переводчика теперь **ссылка** на `/t/[slug]` (резолв: `translator_id → profiles.translator_slug | user_name`, fallback по `ILIKE user_name = author`)

### Что можно выпилить/заменить из TODO прошлых итераций
- ~~`/feed` (лента обновлений) — ссылки ведут в никуда~~ — сделано
- ~~Страница переводчика `/t/alena`~~ — сделано
- ~~Поиск в шапке не работает~~ — работает, простой ILIKE-поиск (без pg_trgm)

## Итерация 7 — «Моя библиотека» + рабочая кнопка закладок

### Новая страница `/bookmarks`
Все закладки пользователя в одном месте, без новых таблиц в БД — всё считается из `profiles.bookmarks` + `profiles.last_read`.

Киллер-фичи:
1. **Умные категории** — 5 вкладок, автораспределение по данным:
   - **Читаю** — last_read не старше 14 дней, прогресс < 100%
   - **На паузе** — last_read 14–90 дней назад, прогресс < 90%
   - **В планах** — в закладках, но не открывал
   - **Прочитано** — прогресс ≥ 90% или новелла завершена + прогресс ≥ 80%
   - **Брошено** — last_read > 90 дней, прогресс < 50%
2. **Темп чтения** — для «На паузе»/«Брошено» карточка считает оставшиеся главы и даёт прогноз: «По 2 гл/день — 27 дней. По 5 гл/день — 11 дней».
3. **Счётчик свежих глав** — золотой бейдж `✨ +5` на обложке, если после твоей последней прочитанной вышли новые.

Сводная строка в шапке страницы: «12 новелл · 3 в активном чтении · ✨ 17 новых глав».

### Рабочая кнопка закладок
- `src/components/BookmarkButton.tsx` — клиентская кнопка с `<details>`-dropdown
- Поддерживает смену статуса (Читаю / В планах / На паузе / Прочитано / Брошено) и удаление
- Пишет в `profiles.bookmarks` в формате `{ [firebase_id]: status }`, совместима с legacy-массивом tene (при чтении конвертирует array → object)

### Интеграции
- В `SiteHeader` добавлена ссылка **«Полка»** (видна залогиненным)
- На странице новеллы статичная `♥ В закладки` заменена на рабочий `BookmarkButton`
- Компонент читает текущий статус из `profiles.bookmarks` и подсвечивает активный пункт в dropdown

## Итерация 8 — регистрация и вход через Telegram, Google, Яндекс, email

### Контекст
Поскольку chaptify и tene **живут на одной БД Supabase**, отдельного переноса аккаунтов не требуется: один раз войдя на chaptify под тем же Telegram/email, пользователь автоматически попадает в свой tene-аккаунт со всеми закладками, монетами, подписками, прогрессом чтения.

### Страницы
- **`/register`** (новая) — регистрация, middleware пускает без авторизации
- **`/login`** (переписана) — та же форма, другой заголовок и дефолтный режим

Оба используют общий компонент `src/components/auth/AuthForm.tsx` (mode = 'login' | 'register').

### Методы входа

1. **Telegram Login Widget** — `src/components/auth/TelegramLoginWidget.tsx`. Подгружает официальный telegram-widget.js, callback отправляется на существующий `auth-service` в репозитории `server` (эндпоинт `/auth/telegram`, принимает `widgetData`). Сервис валидирует HMAC-подпись от Telegram, находит или создаёт аккаунт по `telegram_id`, возвращает Supabase-сессию. Клиент делает `supabase.auth.setSession()`.
2. **Google OAuth** — `supabase.auth.signInWithOAuth({ provider: 'google' })`. Нужен native Google provider включённым в Supabase Auth dashboard.
3. **Яндекс** — кнопка-заглушка. Supabase не имеет native Yandex-провайдера. Реализация потребует custom OIDC в Auth + эндпоинт в auth-service (как Telegram), либо внешний OAuth-proxy. Отложено.
4. **Email + пароль** — стандартный `signUp` / `signInWithPassword`.
5. **Magic link** — `supabase.auth.signInWithOtp({ email })`. Войти по ссылке из письма без пароля.

### 3 киллер-фичи

1. **Telegram в один клик** — главная кнопка сверху, без ввода паролей. Работает через уже существующий рабочий auth-service tene.
2. **Баннер «Есть аккаунт на tene.fun?»** — `src/components/auth/MigrationHint.tsx`. Явно подсказывает пользователю, что можно войти под старыми данными и ничего не потерять.
3. **Magic link** — вкладка «✉ Magic link» в email-форме: ввёл email → получил ссылку → вошёл. Пароль помнить не надо.

### Изменения в репозитории `server`
- `auth-service/index.js`: в `allowedOrigins` добавлены `https://chaptify.ru`, `https://www.chaptify.ru`, `http://localhost:3000`. Один и тот же сервис теперь обслуживает оба сайта.

### Нужно настроить во внешних системах

1. **BotFather (Telegram)**
   - `/mybots` → выбрать бота `@tenebrisverbot` → Bot Settings → Domain
   - Добавить домен `chaptify.ru`. Без этого виджет не загрузится на chaptify.ru.
2. **Google Cloud Console**
   - В существующем OAuth-клиенте добавить `https://chaptify.ru` в Authorized JavaScript origins
   - Добавить `https://<supabase-url>/auth/v1/callback` в Authorized redirect URIs (уже должно быть для tene)
3. **Supabase Auth settings**
   - Site URL добавить `https://chaptify.ru` (через admin API или dashboard)
   - Для magic link: настроить SMTP (или Supabase-встроенный отправитель)
4. **Env vars для chaptify-web** (в .env.production или Portainer):
   - `NEXT_PUBLIC_TG_BOT_USERNAME=tenebrisverbot`
   - `NEXT_PUBLIC_AUTH_API_URL=https://tene.fun` (или где слушает auth-service)

### Изменения middleware
- `/register` добавлен в список публично доступных путей (наравне с `/login`)

## Итерация 9 — аналитика для переводчика

### Новая страница `/admin/analytics`
Переводчик видит, как дышат его новеллы. Для админа — всё то же самое, но по всем переводчикам сразу (без разбиения).

Селектор периода вверху: **неделя / месяц / 3 месяца** (через `?period=7|30|90`).

### 4 основных метрики (в больших карточках)
- Глав выпущено
- Покупок глав
- Монет заработано (сумма покупок)
- Новых подписок (+ отдельно «активных сейчас»)

### Киллер-фичи

1. **Сравнение с прошлым периодом** — `src/components/analytics/PeriodCard.tsx`
   Каждая карточка показывает не только число, но и `▲ +45% (+12)` или `▼ −10% (−3)` — сравнение с аналогичным прошлым периодом. Цветная левая планка: зелёная (рост), розовая (спад), бежевая (без изменений). Видно мгновенно, где хорошо, где надо что-то делать.

2. **Автоматические «моменты»** — `src/components/analytics/TopMoments.tsx`
   Блок «Моменты недели» с 3–5 коротких наблюдений, сгенерированных из данных:
   - 🚀 «Скачок покупок: в 2.3 раза больше, чем за прошлый период»
   - 💝 «+4 новых подписчика»
   - ⏸ «Пауза в выпусках — за неделю ни одной главы»
   - ⭐ «Лунные песни в центре внимания: 3 новых главы, 47 покупок»
   - 🌟 «8 оценок за неделю, средняя — 4.7 ★»

3. **Тепловая карта по новеллам** — `src/components/analytics/NovelsTable.tsx`
   Таблица твоих новелл с ячейками, раскрашенными по интенсивности (5 уровней цвета от бежевого к акценту) относительно самой «горячей» новеллы в списке. Сразу видно, какая новелла тащит, какие проседают. Колонки: всего просмотров · глав за период · покупок · рейтинг · подписчиков.

### Данные, откуда берём
Без новых миграций — всё из существующих таблиц:
- `novels_view.views`, `novels_view.chapter_count`, `average_rating` — текущее состояние
- `chapters` where `published_at` in period — новые главы
- `coin_transactions` where `reason='chapter_purchase'` + `reference_id` начинается с `novel_id:` — покупки (фильтр по новеллам переводчика сделан на уровне приложения)
- `subscriptions` where `translator_id = me` — подписки и новые за период
- `novel_ratings` where `created_at` in period — рейтинги

### Ссылка
В шапке админского дашборда добавлена кнопка **«Аналитика»** рядом с «+ Новелла».

## Итерация 10 — монетизация: пополнение, подписки, пэйвол

### Новые страницы

**`/profile/topup` — пополнение баланса**
Покупка монеток через Tribute или Boosty. На странице:
- Текущий баланс в большой карточке справа от заголовка
- Сетка пакетов (100 / 500 / 1000 / 2500 монет)
- Выбор способа оплаты (Tribute или Boosty)
- Сайдбар с историей операций
- Код платежа для комментария к донату

Киллер-фичи:
1. **«≈ N глав» на каждой коробке** — визуально понятно, что ты получишь (считается по средней цене 10 монет за главу) + **бонусы на крупных пакетах** с процентами (+10% / +15% / +20%)
2. **Умный тип про подписку** — если пакет дороже месячной подписки (299 ₽) и покроет >30 глав, выскакивает подсказка «имеет смысл оформить подписку, а не разовый пакет»
3. **Личный `payment_code`** — код в копилку с кнопкой «скопировать», чтобы вставить в комментарий платежа. Tribute/Boosty webhooks смогут сопоставить платёж с аккаунтом автоматически (сервис `boosty_api` уже есть в server-репо, доделаем в отдельной итерации)

**`/profile/subscriptions` — мои подписки**
Три секции: Активные · Ожидают подтверждения · История. Для каждой:
- Аватар переводчика, имя (ссылка на `/t/[slug]`), провайдер (Tribute/Boosty/Карта), план
- Активные с срок действия, предупреждение «осталось 5 дней» если <7
- Пустое состояние с CTA к каталогу

### Пэйвол для платной главы
`src/components/reader/ChapterPaywall.tsx` — компонент вместо текста главы, когда глава `is_paid=true` и у юзера нет ни подписки, ни штучной покупки.

Логика проверки (`src/app/novel/[id]/[chapterNum]/page.tsx`):
- Если `chapter.is_paid = false` → сразу пускаем
- Иначе вызываем RPC `can_read_chapter(user, novel, chapter)` (миграция 001)
- Если RPC ещё нет в БД (миграция 001 не накачена) → пропускаем всех (безопасный фоллбэк, не ломает tene)

UI пэйвола: две опции рядом
- **Купить за 10 монет** — кнопка вызывает RPC `buy_chapter`; если монет не хватает, кнопка превращается в «Пополнить баланс»
- **Подписаться на переводчика** — с бейджем «Выгоднее», ведёт на `/t/[slug]`

### Интеграции
- В `SiteHeader` появляется бейдж **баланса монет** рядом с кнопкой профиля (если в БД есть колонка `coin_balance`)
- На странице `/profile` три статистические карточки теперь кликабельные: **Баланс → Пополнить**, **Закладки → Библиотека**, **Подписки → Управлять**

### Нужно настроить снаружи
- Tribute/Boosty webhook → вызов RPC `add_coins(user, amount, 'tribute_topup'|'boosty_topup', 'tribute'|'boosty', provider_payment_id)`. Сейчас RPC уже готова, нужно добавить эндпоинты в `boosty_api` или отдельный сервис.
- Env `NEXT_PUBLIC_TRIBUTE_CHANNEL`, `NEXT_PUBLIC_BOOSTY_URL` в chaptify-web (иначе кнопки отключены)

## Итерация 11 — социалка: чужие профили, друзья, сообщения

### Новые страницы
- **`/u/[id]`** — публичный профиль. Параметр `id` может быть `auth.users.id` или `user_name`. На странице: аватар, имя, бейдж роли, общая статистика (всего открытых новелл, в закладках), статус активности.
- **`/friends`** — три секции: Новые запросы / Мои друзья / Ожидают ответа. В каждой карточке аватар, имя, индикатор активности и кнопки действий.
- **`/messages`** — список бесед с последним сообщением, временем, бейджем непрочитанных.
- **`/messages/[userId]`** — собственно чат с конкретным другом. Polling раз в 10 сек (вместо realtime для MVP). Shift+Enter = перенос, Enter = отправить.

### Киллер-фичи

1. **Общие вкусы** (`/u/[id]`) — баннер «🤝 У вас 7 общих новелл» с названиями первых трёх. Считается из `profiles.last_read` обоих. Если совпадений нет и вы друзья — показываем «🌱 Пока нет совпадений, порекомендуй что-нибудь первой».
2. **Поделиться главой в чате** — при отправке сообщения клиент распознаёт ссылку на `/novel/<slug>/<n>` в тексте, лукапит `novels`, привязывает `attached_novel_id + attached_chapter_number`. Сообщение рендерится с превью-карточкой: обложка, название, «Глава N» — одним кликом из чата в главу. Работает прозрачно: просто вставляешь ссылку.
3. **Индикатор «читает сейчас»** — зелёная точка на аватаре и текст «читает сейчас» + ссылка на главу, если `last_read.timestamp` моложе 60 минут. Есть во всех трёх местах: `/u/[id]`, списке друзей, списке чатов, хедере чата. Градация: reading (<1 ч) · recent (<24 ч) · away.

### Миграция 006 — `migrations/006_friendships_messages.sql`

Новые таблицы (RLS включён, INSERT/UPDATE только через security-definer RPC):
- `friendships (id, requester_id, addressee_id, status, created_at, decided_at)` — статусы: pending / accepted / declined / blocked
- `direct_messages (id, sender_id, recipient_id, text, attached_novel_id, attached_chapter_number, created_at, read_at)`

Новые RPC:
- `send_friend_request(p_to)` — создаёт pending; если есть встречный pending, сразу ставит accepted (symmetrical handshake)
- `respond_to_friend_request(p_request_id, p_accept)`
- `unfriend(p_other)`
- `get_friendship_status(p_other)` — возвращает `'none' | 'pending_outgoing' | 'pending_incoming' | 'friends' | 'declined' | 'blocked'`
- `send_direct_message(p_to, p_text, p_novel_id, p_chapter_num)` — проверяет friendship
- `mark_dm_read(p_other)`
- `list_conversations()` — возвращает собеседников с last_text + unread_count одним запросом
- `unread_dm_count()` — суммарный счётчик

### Интеграции
- `SiteHeader` получает два новых пункта: **«Друзья»** и **«Сообщения»** (с бейджем непрочитанных). Безопасный фоллбэк: если RPC `unread_dm_count` ещё нет в БД, просто не показываем бейдж.
- `src/lib/social.ts` — общие типы и хелпер `detectReadingNow()` для градации активности.

## Итерация 12 — аудит багов: комментарии, миграции, proxy

### Починено

**`CommentsSection` переписан на реальную схему tene**
Старый компонент ссылался на несуществующие поля (`chapter_id`, `content`, `profiles.username`) — при любом заходе в главу вылетал с ошибкой. Теперь использует:
- `comments.novel_id` + `comments.chapter_number` (не `chapter_id`)
- `comments.text` (не `content`)
- `comments.user_name`, `user_avatar_url`, `user_avatar`, `is_vip` — денормализованные поля, заполняются при insert
- `comments.reply_to` — для вложенных ответов
- `comments.like_count` + отдельная таблица `comment_likes` — для сердечек

На странице главы обновлён вызов: было `<CommentsSection chapterId={...} />`, стало `<CommentsSection novelId={...} chapterNumber={...} />`.

3 киллер-фичи комментариев (раз уж переписывать):

1. **Лайки через `comment_likes`** — кнопка ❤ с счётчиком, оптимистичный апдейт (сразу видно изменение, запрос летит параллельно). Юзер может лайкнуть/раз-лайкнуть.
2. **Вложенные ответы** — нажимаешь «Ответить» под комментарием, открывается inline-форма; отправка создаёт запись с `reply_to`. Ответы рендерятся с левой полоской-отступом. Глубже одного уровня не идём (визуально было бы каша).
3. **Спойлер-синтаксис Reddit-style** — `>!скрытый текст!<`. В рендере это чёрный замазанный прямоугольник с текстом «спойлер» — по клику открывается. Важно для обсуждения поворотов сюжета без разрушения чужого опыта.

Hint про синтаксис показан под textarea.

**Миграции 001–006 → идемпотентные**
Теперь безопасно запускать повторно — никаких ошибок «already exists» не возникнет:
- Миграция 001: `CREATE TYPE user_role` обёрнут в `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$`
- Миграция 001: `UPDATE novels SET translator_id` теперь ищет переводчика по `is_admin = true OR user_name = 'alena'` вместо жёсткого `user_name = 'alena'` — работает даже если у тебя другой user_name
- Все `CREATE POLICY` в 003, 004, 005, 006 теперь с `DROP POLICY IF EXISTS ... ON ...;` перед ними

**`middleware.ts` → `proxy.ts`**
Next.js 16 объявил `middleware` устаревшим. Переименовал файл и функцию (`middleware` → `proxy`). Конфигурация матчеров не изменилась. Deprecation-warning из билд-лога ушёл.

### Что ещё осталось (на будущее)

- Tribute/Boosty webhooks в `boosty_api` (сейчас RPC `add_coins` готова, только сервис дёргать)
- Custom OIDC для Яндекс-авторизации
- Realtime через Supabase вместо polling в чатах (когда появится нагрузка)
- Страницы «О проекте», «FAQ», «Политика конфиденциальности»
- Dashboard выплат переводчику (`/admin/payouts`)
- Центр уведомлений (таблица `notifications` уже есть в tene)
- Массовая загрузка глав через .zip

## Итерация 13 — центр уведомлений

### Новая страница `/notifications`
Таблица `public.notifications` уже существовала в tene — расширил её и добавил автоматическое наполнение через триггеры. Старые уведомления из tene продолжают работать как были; новые события накидываются триггерами сами.

### Автоматические события (триггеры БД)
Миграция 007 вешает триггеры, которые создают уведомления без вызова из кода:

- **Новое сообщение** → триггер `trg_notify_new_message` на `direct_messages INSERT` → уведомление получателю
- **Запрос в друзья** → триггер `trg_notify_friend_request` на `friendships INSERT` (status=pending) → addressee видит запрос
- **Запрос принят** → триггер `trg_notify_friend_accepted` на `friendships UPDATE` (pending → accepted) → уведомление отправителю
- **Ответ на комментарий** → триггер `trg_notify_comment_reply` на `comments INSERT` (reply_to NOT NULL) → уведомление автору родительского
- **Новая подписка** → триггер `trg_notify_new_subscription` на `subscriptions INSERT` (status=active) → уведомление переводчику

Каждый триггер тянет красивое имя актора (`translator_display_name ?? user_name`), формирует target_url и group_key. Никаких изменений в существующем коде tene не нужно — триггеры работают прозрачно.

### Добавленные колонки в notifications
- `actor_id uuid` — кто вызвал событие (для аватара)
- `group_key text` — для автогруппировки похожих событий
- `ref_novel_id bigint` — опциональная связь с новеллой

### Три киллер-фичи

1. **Автогруппировка** — RPC `list_notifications` через `DISTINCT ON (group_key)` возвращает один элемент на группу + `group_count`. В UI: «Иван написал сообщение · и ещё 4». Не засоряет ленту одинаковыми.
2. **Фильтры по типу** — табы «Все / Непрочитанные / Друзья / Сообщения / Комментарии / Подписки». Через `?filter=` в URL.
3. **Inline-кнопки** — для уведомления о запросе в друзья прямо в карточке есть «Принять» / «Отклонить» — не нужно переходить в `/friends`. Один клик — и готово.

### Интеграции
- `SiteHeader` получает колокольчик 🔔 с бейджем непрочитанных (отдельно от «Сообщений»)
- RPC `unread_notifications_count` вызывается в шапке с безопасным try/catch
- Кнопка «Прочитать всё» в заголовке страницы

### Служебные RPC
- `list_notifications(p_limit, p_only_unread)` — с автогруппировкой + аватары авторов
- `unread_notifications_count()` — для бейджа в шапке
- `mark_all_notifications_read()` — массовая пометка

## Итерация 14 — большой багфикс + BB-коды + массовая загрузка глав

### Баг-фиксы по списку пользователя

1. **Обложки** — `getCoverUrl` умный резолвер: если имя файла выглядит как UUID или `<timestamp>-<rand>.ext` (новые загрузки) — идёт в `/storage/v1/object/public/covers/`, иначе — по старому пути `/covers/имя.ext`. Старые tene-обложки и новые из UI работают одновременно.
2. **Переводчик вместо автора** — на странице новеллы блок «Переводчик» теперь берёт профиль из `profiles` по `novels.translator_id` (с fallback `ILIKE user_name = author`). Имя/аватар — из `translator_display_name` / `translator_avatar_url`.
3. **Кликабельные жанры** — `<span class="tag">` → `<Link class="tag tag--link" href="/catalog?genre=X">`. Hover-состояние в CSS.
4. **Закладки** — `BookmarkButton` больше не пишет в `profiles` напрямую (RLS запрещает), а идёт через RPC `update_my_profile(data_to_update jsonb)` из tene. Та же починка для `saveProgress` в читалке — `last_read` тоже писался мимо RLS.
5. **CORS для `/rest/v1/comments` (и т.п.)** — поправлен `server/nginx-proxy/nginx.conf`:
   - Добавлен `map $http_origin $cors_origin {...}` — разрешает только tene.fun / chaptify.ru / localhost
   - В блоке `/(auth/v1|rest/v1|storage/v1)` обрабатывается preflight (OPTIONS→204) и выставляются CORS-заголовки
   - В список `Access-Control-Allow-Headers` включены `accept-profile`, `content-profile`, `x-supabase-api-version`, `prefer`, `range` и другие — именно их шлёт новый `@supabase/ssr`
   - Убран жёсткий `proxy_set_header Accept 'application/json'` (ломал PostgREST negotiations)
   - Добавлен отдельный `server { server_name chaptify.ru; }` блок, проксирующий на контейнер `chaptify-web`
6. **Буквица (drop-cap)** — удалён `.novel-content p:first-child::first-letter` из `globals.css`.
7. **Фокус-режим** — переписан: раньше CSS-селектор `:nth-child(${activeParagraph + 1})` делался через styled-jsx (не подхватывал обновления). Теперь JS просто навешивает класс `.focus-active` на текущий абзац и снимает с предыдущего; CSS блока: всем 0.28, активному 1.
8. **Таймер сна** — разделил состояния: `selectedPreset` (для подсветки кнопки) не тикает; `sleepMinLeft` уменьшается от пресета до 0. Выделяется теперь **только** выбранный пресет, а не все что ≥ него.

### Форма добавления новелл

9. **Жанры — только из списка** — убрал поле ввода своего жанра. `PREDEFINED_GENRES` расширен с 23 до 50+ (добавил Научная фантастика, Киберпанк, Стимпанк, Исэкай, Культивация, Реинкарнация, LitRPG, Гарем/Обратный гарем, Восточная древность, Магическая академия и т.д.). Валидация: минимум 1 жанр.
10. **Разделение имени автора на 3 поля** — мигарция 008 добавила `novels.author_original` + `novels.author_en` (уже есть `novels.author`). Форма: 3 поля в ряд (оригинал / английский / русский). На странице новеллы выводится как `оригинал / английский / русский` через разделитель.
11. **Подсказки по наведению** — у каждого поля формы `title="..."` с объяснением для человека не в теме («оригинал», «почему год оригинала, а не перевода», «что значит возрастное ограничение»). Плюс мелкие `.form-hint` под проблемными полями.

### BB-коды (вместо голых HTML-тегов)

12. **`src/lib/bbcode.ts`** — парсер `[b] [i] [u] [s] [h] [quote] [center] [spoiler]` → HTML. Плюс `htmlToBb()` для обратного преобразования (нужно при edit-mode, где из БД приходит HTML).
13. **`src/components/admin/BBCodeEditor.tsx`** — редактор с toolbar: клик по кнопке оборачивает выделение тегом. Горячие клавиши Ctrl+B / Ctrl+I. Живой предпросмотр справа.
14. **В форме описания новеллы** — заменил голый `<textarea>` на `BBCodeEditor`. В БД пишется **готовый HTML** (конвертируется через `bbToHtml` при submit). При edit — из БД приходит HTML, конвертируется обратно в BB через `htmlToBb`.
15. **В форме текста главы** — то же самое. Split-view со старой preview-панелью убран (лишнее усложнение); теперь один BB-редактор со встроенным предпросмотром в кнопку. Стата считается от HTML-версии.

### Массовая загрузка глав

16. **`/admin/novels/[id]/chapters/bulk`** — новая страница.
    - Инструкция сверху: «Начало каждой главы пометь `Глава 1`, `Глава 2` и т.д.»
    - BB-редактор для вставки всего текста сразу
    - Автоматический сплит по regex заголовков (`/^\s*(Глава|Chapter)\s+(\d+)/gim`)
    - Онлайн-предпросмотр разбиения (список «Глава X · 3421 слов · бесплатно/платно»)
    - Поля: начальный номер (если заголовков нет), «с какой главы платные», дефолт платности
    - Прогресс-бар при загрузке («Глава 3 из 12»)
    - По одной подряд: `storage.upload` → `chapters.upsert` (безопасно при повторной загрузке)
    - `novels.latest_chapter_published_at` обновляется в конце
    - Ссылка «📚 Массовая загрузка» добавлена в шапку `/admin/novels/[id]/edit`

### SQL миграция 008 — `migrations/008_author_variants.sql`
- `ALTER TABLE novels ADD COLUMN IF NOT EXISTS author_original text`
- `ALTER TABLE novels ADD COLUMN IF NOT EXISTS author_en text`
- `DROP + CREATE VIEW novels_view` с дополнительными колонками `author_original`, `author_en`, а также ранее добавленными `title_original`, `title_en`, `country`, `age_rating`, `translation_status`, `release_year`, `moderation_status`

### Изменения в `server/nginx-proxy/nginx.conf` (важно!)
Новая версия уже лежит в репо `server`. Чтобы починить CORS на проде:
1. Скопировать новый `nginx.conf` на сервер
2. Проверить что SSL-сертификат для chaptify.ru уже есть в `/etc/letsencrypt/live/chaptify.ru/` (если нет — `certbot --nginx -d chaptify.ru -d www.chaptify.ru`)
3. `docker exec nginx-proxy nginx -t` — проверить синтаксис
4. `docker exec nginx-proxy nginx -s reload` — перезагрузить без downtime

## Итерация 15 — новости от админа + фикс обложек

### Починка обложек
В старой БД tene `novels.cover_url` для многих записей хранится с префиксом `covers/11.webp` (т.е. путь уже содержит папку). Мой `getCoverUrl` добавлял ещё один префикс `/covers/`, получая `/covers/covers%2F11.webp` (слэш кодировался как `%2F`).

Исправил в `src/lib/format.ts`: если путь начинается с `covers/` — не добавляем префикс повторно, только кодируем имя файла после слэша. Остальная логика (UUID → storage, обычное имя → /covers/) сохранена. Старые tene-обложки и новые загрузки работают одновременно.

### Новости админа

**Новые страницы:**
- `/news` — публичная лента всех новостей с фильтром по типу
- `/news/[id]` — отдельная новость (с поддержкой черновиков для админа)
- `/admin/news` — список новостей с кнопкой создания (админам)
- `/admin/news/new` — создать
- `/admin/news/[id]/edit` — редактировать

**Блок на главной** (`LatestNews`) — до 3-х свежих/закреплённых новостей, между `MyShelfStrip` и `MoodPicker`. Если нет ни одной — блок не показывается.

**Шапка:** добавлен пункт «Новости» с бейджем непрочитанных (рядом с Лентой).

### 3 киллер-фичи

1. **5 типов новостей с цветами** — 📢 Объявление (акцентный), 🎉 Событие (золотой), ⚡ Обновление (зелёный), 💡 Совет (серый), 🔧 Тех. работы (розовый). Плюс **📌 Pinned** — закреплённые всплывают наверх ленты, независимо от даты.

2. **Привязка к новелле** — в форме новости автокомплит по названиям: нашёл нужную → один клик, она привязана. В рендере к новости приклеивается мини-карточка с обложкой + «Открыть →». Удобно для «Запустили новую новеллу!» или «Скидка на главы X».

3. **Трекинг непрочитанных** — RPC `mark_news_seen(max_id)` сохраняет `news_seen_max_id` в `profiles.settings jsonb` (обходя RLS через SECURITY DEFINER). RPC `unread_news_count()` считает, сколько появилось новых после последнего визита. Визит на `/news` или `/news/[id]` (через клиентский `MarkSeenOnMount`) сбрасывает счётчик. В шапке и на главной — живой бейдж «3 новых».

### Миграция 009 — `migrations/009_news.sql`
- Таблица `news_posts`: id, author_id, title, body (HTML), type (5 enum-values), is_pinned, is_published, attached_novel_id, created_at/published_at/updated_at
- RLS: read для всех опубликованных (+ для админа его черновики); INSERT/UPDATE/DELETE только админу
- Триггер `updated_at`
- RPC: `mark_news_seen(p_max_id)`, `unread_news_count()`

### BB-редактор в форме новостей
`NewsForm` использует тот же `BBCodeEditor`, что и для описаний новелл и глав. Админ не пишет HTML-теги — только тыкает кнопки или простой текст.

## Итерация 16 — per-translator выплаты + FAQ

### Архитектура выплат

**Tribute — автоматически.** У Tribute есть честные вебхуки. Каждый переводчик получает свой уникальный URL `https://tene.fun/webhook/tribute/<token>`, который копирует в настройки Tribute. Когда читатель платит, прилетает POST с данными (tg_id, сумма, комментарий). Мы находим переводчика по токену, читателя — по `payment_code` в комментарии или по `telegram_id`, и начисляем монеты / активируем подписку.

**Boosty — руками.** Официального API нет. Все сторонние библиотеки работают через скрейпинг с куками залогиненного пользователя. Куки протухают за месяц, держать их в БД — риск. Пока что Boosty просто внешняя ссылка; подписка активируется вручную по коду в комментарии к донату. Позже можно добавить опциональную авто-сверку через куки.

### Новые страницы

**`/admin/payouts`** — настройки способов выплат:
- Tribute-блок: уникальный webhook URL с кнопкой «Скопировать», health-индикатор (зелёная/жёлтая/красная точка по `payout_last_tribute_event_at`), инструкция пошагово, кнопка перегенерировать токен.
- Boosty-блок: поле с URL на свою Boosty, объяснение почему нет авто-сверки.
- Блок про коды платежей.

**`/help`** — FAQ в стиле tribute.tg:
- Инстант-поиск по вопросам (фильтрация по ключевым словам)
- 5 разделов-табов: Читателям / Переводчикам / Оплата / Аккаунт / Техподдержка
- 18 готовых вопросов-ответов с внутренними ссылками и якорями
- CTA «Не нашла ответ?» с кнопкой в Telegram-бот

Ссылка на `/help` добавлена в футер.

### 3 киллер-фичи `/admin/payouts`
1. **Health-индикатор**: webhook URL с живой меткой «Активен · 5 мин назад» / «Тихо 12 дней» / «Похоже, отключён». Считается из `payout_last_tribute_event_at`, бот обновляет после каждого webhook'а.
2. **Перегенерация токена одним кликом** — RPC `regenerate_tribute_webhook_token`, старый URL мгновенно инвалидируется.
3. **Пошаговая инструкция** в раскрывающемся `<details>` прямо на странице — не надо уходить в отдельную доку.

### 3 киллер-фичи `/help`
1. **Инстант-поиск** — печатаешь «монеты» → остаются только релевантные вопросы, без перезагрузки страницы.
2. **Табы категорий** — один клик фильтрует только вопросы переводчиков / только про оплату.
3. **Collapsible Q&A с якорями** — каждый вопрос имеет `id`, можно кинуть прямую ссылку типа `/help#pay-methods`.

### Миграция 010 — `migrations/010_payout_tokens.sql`
- Поля в `profiles`:
  - `payout_tribute_webhook_token` — 24-байтовый URL-safe токен
  - `payout_tribute_secret` — 32 байта для HMAC-подписи
  - `payout_last_tribute_event_at` — для health-индикатора
  - `payout_boosty_url`, `payout_boosty_cookies_enc`, `payout_last_boosty_sync_at`
- `pgcrypto` (обычно уже есть)
- Автогенерация токенов существующим переводчикам (UPDATE по `is_admin`/`role`)
- Триггер: при назначении `role='translator'` токены создаются сами
- RPC: `regenerate_tribute_webhook_token` (из UI), `get_translator_by_webhook_token` (для бота, service_role), `mark_tribute_event`

### Python webhook-обработчик (server/my-bot/)
**`tribute_chaptify_webhook.py`** — готовый FastAPI-модуль:
- Route `POST /webhook/tribute/{token}`
- Валидация HMAC по `payout_tribute_secret`
- Поиск переводчика по токену через RPC
- Поиск читателя: по `payment_code` в комментарии → fallback `telegram_id`
- События: `new_donation` → `add_coins`; `new_subscription` → `upsert subscriptions`; `cancelled_subscription` → статус
- Если читатель не нашёлся — запись в `pending_tributes` для ручной сверки
- `mark_tribute_event` для health

**Чтобы подключить** в `server/my-bot/bot.py`:
```python
from tribute_chaptify_webhook import register_tribute_chaptify_routes
register_tribute_chaptify_routes(app, supabase)
```

### nginx-proxy изменения
Добавлена location `/webhook/tribute/` → порт 8080. Не трогает существующий `/tribute/` (личный Tribute Алёны).

### Как запустить
1. Накатить миграцию 010 на Supabase
2. В `bot.py` зарегистрировать новый route (2 строки)
3. Скопировать обновлённый `nginx.conf` на хост → `nginx -s reload`
4. Каждому переводчику: `/admin/payouts` → скопировать URL → вставить в настройки Tribute → выбрать события (new_subscription, cancelled_subscription, new_donation)

## Итерация 17 — учёт выплат переводчикам + фикс 007

### Фикс миграции 007
`list_notifications` RPC ссылался на `p.user_avatar_url`, которого в схеме tene нет (есть только `translator_avatar_url` после миграции 001). Убрал fallback, осталось одно поле. Миграция идемпотентная — можно перенакатить её целиком.

### Почему был важен вопрос «кому идут монетки»

До этой итерации система жила по модели «у юзера один кошелёк, у переводчика тоже» — это действительно странно, потому что непонятно, как разделять деньги между несколькими переводчиками.

**Правильная модель (как у Patreon/Steam/Yandex Music):** деньги физически лежат у платформы, переводчикам **причитаются** виртуальные суммы, которые платформа раз в месяц закрывает реальными переводами.

Это лучше для всех:
- Читатель один раз пополняет — тратит где хочет, без повторного ввода карты
- Переводчик не регистрирует платёжки на себя лично
- Налоги централизованные
- Платформа гибко настраивает комиссии/курсы

### Миграция 011 — `migrations/011_payout_cycles.sql`

Таблица `payout_cycles (translator_id, period_from, period_to, coins_gross, coins_net, platform_fee_pct, rub_rate, amount_rub, payout_method, payout_ref, paid_at, ...)`. RLS: переводчик видит только свои, админ — все.

RPC:
- `translator_earnings_raw(translator_id, from, to)` — сколько монет заработано за период
- `translator_earnings_pending(translator_id)` — сколько накоплено с конца последнего закрытого цикла до сейчас (умно учитывает что админ уже закрыл)
- `all_translators_earnings(from, to)` — сводка по всем переводчикам для админа (только админ)

### Новая страница `/admin/all-payouts` (только админ)

- Селектор месяца (12 последних)
- Итого монет и ₽ за период
- Настраиваемые курс и комиссия прямо на странице (по дефолту 1₽/монета, 0%)
- Таблица: переводчик, монеты, ₽, глав, читателей, способ выплат, статус
- **Киллер #1**: CSV-экспорт для удобства перевода денег руками
- **Киллер #2**: кнопка «Выплачено ✓» на каждом ряду — создаёт запись в `payout_cycles`, строка серая
- **Киллер #3**: живые настройки курса и комиссии — меняешь → пересчитываются все суммы

### Обновления

**`/admin/analytics`** для переводчика получил блок **«💰 К выплате»** сверху — сколько монет накоплено со времени последнего цикла, сколько глав куплено, сколько уникальных читателей заплатили.

**Админ-дашборд** получил кнопку **«💰 Выплаты переводчикам»** (видна только админу).

### FAQ обновлён
Добавлены и переработаны разделы:
- «Как устроены монеты и как я получаю деньги?» — объяснение про split-модель, почему так
- «Когда приходят деньги?» — график выплат (раз в месяц), способы, минимальная сумма

### Про быстрый онбординг Boosty — честный ответ в FAQ
Рекомендация новым переводчикам: **бери Tribute**, настройка 2 минуты, работает автоматически. Boosty — только если уже накоплена аудитория там; авто-сверки не будет, подтверждаешь платежи вручную по коду в комментарии. Это записано в ответах раздела «Переводчикам».

## Итерация 18 — настройки профиля

### Новая страница `/profile/settings`
Всё, что юзер меняет сам, без помощи админа:
- Аватар (через `AvatarPicker` — см. ниже)
- Отображаемое имя (`user_name`)
- Email / Telegram ID — read-only с пометкой, что меняются через бота или magic-link
- Переключатель приватности «Показывать мою историю чтения другим» (`profiles.settings.show_reading_publicly`)
- Для переводчика — отдельная секция: `translator_display_name`, отдельный аватар публичной страницы, `translator_about`, `payout_boosty_url`

Ссылка «⚙ Настройки» добавлена в `/profile`.

### 3 киллер-фичи

1. **Три способа выбрать аватар** (`src/components/AvatarPicker.tsx`):
   - **Загрузить файл** — до 3 МБ, jpg/png/webp, в bucket `avatars/<user_id>/<timestamp>-<rand>.ext`
   - **Выбрать готовый** — 8 предустановленных градиентных пресетов с твоей первой буквой. One-click для тех, кто не хочет искать фото.
   - **Из Telegram** — если юзер логинился через TG, `photo_url` из `user_metadata` подтягивается. Одна кнопка «Использовать это фото».
2. **Круглое превью в реальном времени** — меняешь аватар → сразу видишь, как будет выглядеть в круглой рамке. Без сохранения.
3. **Приватность чтения** — переключатель реально действует: на `/u/[id]` скрываются `last_read`, закладки, «читает сейчас», совпадения по вкусам. Владелец и админ видят всегда.

### Миграция 012 — `migrations/012_profile_settings.sql`
- `profiles.avatar_url text` — универсальное поле аватара
- RPC `update_my_settings(data_to_update jsonb)` — security-definer, обновляет строго безопасный набор полей (`user_name`, `avatar_url`, `translator_*`, `payout_boosty_url`, `settings`). RLS на profiles продолжает запрещать прямой UPDATE.
- Bucket `avatars` с public SELECT и INSERT/UPDATE/DELETE только в свою папку (`avatars/<auth.uid()>/...`)

### Новые компоненты
- `src/components/UserAvatar.tsx` — универсальный рендер аватара. 3 режима: `image` (URL), `preset` (CSS-градиент), `initial` (первая буква на градиенте). Используется в `SiteHeader`, `/profile`, `/u/[id]`.
- `src/components/AvatarPicker.tsx` — пикер с тремя табами.
- `src/lib/avatar.ts` — `describeAvatar()`, `resolveAvatarUrl()`, `AVATAR_PRESETS` палитра.

### Интеграции
- `SiteHeader`: рядом с именем теперь мини-аватар 24px
- `/profile`: статичная big-avatar → `<UserAvatar size=84>`. Кнопка «⚙ Настройки» рядом с «Админка».
- `/u/[id]`: аватар из `translator_avatar_url ?? avatar_url`, уважает `show_reading_publicly`

### Что ещё стоит сделать следом
Пройти по `/friends`, `/messages`, `ContinueReadingShelf`, `NotificationsClient` — везде заменить руками нарисованные инициалы на `<UserAvatar>` для консистентности.

## Итерация 19 — фиксы фокуса, 18+ gate, новая главная

### Исправлены баги
1. **Фокус-режим в читалке** — действительно не работал. Причины: CSS `.novel-content > .focus-active` не покрывал параграфы внутри `<blockquote>`; класс не навешивался на активный абзац при первом включении без скролла. Переписал: селекторы без `>`, `!important` для `.focus-active`, `MutationObserver` на `.reader-wrapper` пересчитывает активный абзац при toggle класса `.focus-mode`. Теперь работает с первой секунды.
2. **Возраст в жанрах** — отделён. В `CatalogFilters` появилась секция «Возрастное ограничение». Каталог фильтрует через колонку `age_rating`. В агрегации жанров возрастные токены (`18+`, `6+` и т.п.) вырезаются regex `/^\d{1,2}\+$/`. В форме новеллы — подсказка «возраст отдельным полем выше».
3. **18+ gate** — `AdultGate` показывается на странице `18+` новеллы. «Мне есть 18 — продолжить» / «я младше — закрыть». Галочка «больше не спрашивать на сайте» сохраняет подтверждение в `localStorage` + `profiles.settings.adult_confirmed_at`. На карточке 18+ новеллы — красный бейдж.

### Переделана главная страница
Убраны: `WeeklyHero` (мёртвые цифры), блок «переводчики».

Добавлены три новых блока:
- **`ReadingNow`** — hero «Сейчас читают»: обложки новелл, открытых в последние 30 минут. Агрегирую из `profiles.last_read`. На каждой обложке badge с числом читателей и зелёная пульсирующая точка. Клик → на ту главу, на которой сейчас большинство.
- **`CommentsFeed`** — «О чём сейчас говорят»: 8 свежих комментариев из всех глав. Спойлеры `>!...!<` автоматически заменяются плашкой «•••• спойлер ••••». Клик → переход к комментарию через якорь.
- **`NovelPoll`** — голосование «Что переводить следующим?». Прогресс-бары внутри опций, обложка-плейсхолдер, ссылка на оригинал. RPC `cast_poll_vote` — голос / переголос.
- **Тултип на карточках** (`NovelCard`) — при hover появляется popover с описанием (чистится от HTML), жанрами, возрастом, рейтингом. Медиа-запрос `(hover: none)` скрывает на мобильных.

### Миграция 013 — `migrations/013_polls_and_early_access.sql`

**Голосование:**
- `polls (id, title, description, author_id, is_active, ends_at)` — read все, write translator/admin
- `poll_options (id, poll_id, title, description, cover_url, external_link, sort_order)`
- `poll_votes (poll_id, user_id, option_id)` с PK (poll_id, user_id) — один голос на юзера
- RPC `poll_results(poll_id)` — опции с процентами
- RPC `cast_poll_vote(poll_id, option_id)` — голосовать / переголосовать

**Ранний доступ к главам (супер-фича для переводчика):**
- `chapters.early_access_until timestamptz` — если в будущем, главу видят только подписчики переводчика или купившие. После — обычная логика платных глав.
- `can_read_chapter` обновлён — учитывает новый флаг.

### Ещё будет (на будущее)
- UI переводчику, чтобы задавать `early_access_until` через форму главы (сейчас только SQL)
- Админ-UI для создания опросов (сейчас SQL)
- Две ещё супер-фичи: «сколько читателей сейчас в конкретной главе» и drop-off аналитика «на какой главе бросают»

## Итерация 20 — фиксы по жалобам + stories, quote of the day, админка опросов

### Фиксы из жалоб
1. **Прогресс-бар в «Продолжить чтение» прыгал** — на длинных заголовках карточка растягивалась. У `.continue-card .body .title` теперь фиксированная высота `calc(1.3em * 2)` (всегда 2 строки), прогресс-бар в низу карточки держит позицию.
2. **Поиск в шапке схлопывался** — `.main-nav` перешёл на `flex-wrap: wrap` с `row-gap: 6px`, у `.search-box` появился `flex: 1 1 240px; min-width: 200px; max-width: 420px`. Теперь на узких экранах просто переносится на вторую строку, не теряя ширину.
3. **Теги в уведомлениях** (`<b>`, `<br>` от tene-бота) — `NotificationsClient` теперь экранирует весь текст и потом точечно раскрывает только `<b>`, `<strong>`, `<i>`, `<em>`, `<br>`. Всё остальное — безопасный текст.
4. **404 по клику на уведомление** — добавил whitelist префиксов (`/novel/`, `/news/`, `/messages/`, `/friends`, `/u/`, `/admin/`, `/profile`, `/bookmarks`, `/catalog`, `/search`, `/translator/`, `/t/`). Легаси-URL от tene, которые не попадают в whitelist, просто перестают быть кликабельными — вместо 404.
5. **Спойлеры в комментариях на главной** — `CommentsFeed` теперь вообще не показывает текст комментария, только мета: «Автор → «Новелла» · гл. N». Никаких случайных спойлеров от вырванных строк. Клик ведёт прямо в тред главы.
6. **Огромные обложки в «Сейчас читают»** — `.reading-now-grid` перешёл на `minmax(92px, 110px)` с `justify-content: start`. В ряд помещается 6–8 маленьких обложек, а не 3 во весь экран.

### Новые блоки на главной
- **`StoriesStrip`** — Instagram-стиль ленточка сверху, круглые «истории» с градиентом-рамкой. Данные из уже существующей `public.stories` (таблица была в tene). Клик — модалка с картинкой, текстом и CTA. Использую `image_url`, `bg_gradient`, `action_link`, `button_text`.
- **`QuoteOfTheDay`** — случайная публичная цитата из `user_quotes`. Атмосферный блок с большой кавычкой-серифом, италиком, ссылкой на главу-источник.

### Миграция 014 — `migrations/014_public_quotes.sql`
- `user_quotes.is_public boolean` (default false) — чтобы читатели сами решали, делиться ли сохранёнкой
- Индекс `idx_user_quotes_public` (partial, только `is_public = true`)
- RLS-policy `user_quotes_public_read` (SELECT для всех, включая `anon`)
- RPC `random_public_quote()` — берёт случайную из 200 самых свежих публичных + data по автору и новелле

### Админка опросов
- `/admin/polls` — список опросов с числом голосов и статусом («активен», «выключен», «завершён»)
- `/admin/polls/new` — создание опроса
- `/admin/polls/[id]/edit` — редактирование опроса + его вариантов
- `PollForm` — динамический список вариантов (минимум 2), каждый со своим `title`, `description`, `cover_url`, `external_link`. При сохранении: удаляются снятые опции, обновляются существующие, вставляются новые.
- В `/admin/news` и `/admin/polls` добавлены `admin-subtabs` — табы между разделами, так что переключаешься не через breadcrumbs.
- В хедере админки — новая кнопка «🗳 Опросы» рядом с «📢 Новости».

## Итерация 21 — редизайн сториз + «Журнал» (Литрес-стиль)

### Редизайн сториз
Старые круги на разноцветных градиентах не вписывались в общий тёплый бумажный дизайн сайта. Переделал в «карточки-закладки»: маленькие горизонтальные тайлы 172×58 с лёгкой тенью, тонкой рамкой и тёплыми акцентными тонами. Если есть `image_url` — полная заливка картинкой с тёмной вуалью слева (чтобы текст читался); если нет — мягкий акцентный фон. Модалка тоже переодета: `--surface` вместо чёрного фона, серифный заголовок, тёплые тона.

### Параллельно: блок «Журнал»
По примеру Литрес.Журнал — горизонтальный слайдер статей / обзоров / интервью. Карточка: обложка 16:10, рубрики-теги, серифный заголовок, подзаголовок. Клик → страница материала с hero-обложкой.

### Миграция 015 — `migrations/015_news_journal.sql`
- Расширяем `news_posts` полями: `cover_url`, `subtitle`, `rubrics text[]`
- Новые значения `type`: `article`, `review`, `interview` (через пересоздание `news_posts_type_check`)
- Частичный индекс `idx_news_posts_journal` только на журнальных типах

### UI
- `JournalStrip` на главной — читает `news_posts` с `type in (article,review,interview)`, `is_published=true`, до 10 штук. Из общей ленты `LatestNews` эти типы теперь исключены, чтобы не дублировались.
- `/news/[id]` — для журнальных типов сверху hero-обложка (`aspect-ratio: 21/9`), плашки-рубрики и серифный italic subtitle. Обычные новости рендерятся как раньше.
- `NewsForm` — три новых поля (cover_url, subtitle, rubrics chips-редактор) показываются только при выборе типа `Статья`/`Обзор`/`Интервью`.
- `NEWS_TYPES` дополнен тремя новыми типами с эмодзи 📝 🔎 🎙 и tone-palette.

## Итерация 22 — флоу модерации новелл

### Флоу
1. Переводчик создаёт новеллу → она сразу в статусе `draft`, видит только он и админ.
2. Переводчик дополняет карточку, пока не готова → жмёт «На модерацию» на своей странице админки → новелла уходит в `pending`, админы получают уведомления.
3. Админ открывает `/admin/moderation` → видит очередь `pending` с обложкой, описанием, автором, рубриками → «Одобрить» (`published`, триггер шлёт уведомление переводчику) или «Отклонить» (просит причину, кладёт в `rejection_reason`, триггер шлёт уведомление с текстом причины).
4. Если отклонено — переводчик видит причину на карточке и может снова нажать «На модерацию» после правок.

### Миграция 016 — `migrations/016_novel_moderation_flow.sql`
- `novels.reviewed_at`, `novels.reviewer_id` (аудит кто и когда решил)
- RPC `submit_novel_for_review(p_novel)` — только из `draft/rejected` в `pending`, проверяет что вызывающий — владелец или админ
- RPC `review_novel(p_novel, p_approve, p_note)` — только админ, требует причину при отклонении (≥3 символа)
- Триггер `on_novel_moderation_change`: `pending` → уведомление всем админам (`novel_pending`), `pending→published` или `pending→rejected` → уведомление переводчику (`novel_approved` / `novel_rejected`)
- Пересоздан `novels_view` с колонками `rejection_reason`, `reviewed_at`, `reviewer_id`

### UI
- `NovelForm` — новые новеллы у переводчика создаются как `draft`, админ создаёт сразу `published` (проп `isAdmin`)
- `/admin` (дашборд переводчика) — статус-плашка (`mod-badge`) на каждой карточке новеллы: «Черновик» / «На модерации» / «Опубликовано» / «Отклонено». Для `rejected` — панель с причиной отказа. Для `draft`/`rejected` — кнопка «На модерацию» (`SubmitForReviewButton`).
- `/admin` для админа — кнопка «🛡 Модерация» в шапке с бейджем количества `pending`
- `/admin/moderation` — очередь на модерации (`ModerationCard`): обложка, описание, переводчик, рубрики, кнопки «Одобрить» / «Отклонить» с inline-textarea для причины. Снизу — лог последних 10 решений.

### Фильтрация от читателей
Добавил `.eq('moderation_status', 'published')` в публичные запросы:
- `/catalog` (основной запрос + счётчики жанров)
- `/` главная (popular/recent/allNovelsRaw для жанров)
- `/feed` (новеллы для глав)
- `/t/[slug]` (публичный профиль переводчика)
- `/novel/[id]` — если не published и вызывающий не переводчик/админ → `notFound()`
- `/novel/[id]/[chapterNum]` — та же проверка перед загрузкой главы

Личные списки (bookmarks, last_read, continue reading) не фильтрую — если у читателя новелла уже в закладках, пусть остаётся видна.

## Итерация 23 — расписание переводчика

### Фича
Переводчик может разложить свои новеллы по дням недели (Пн–Вс). Читатели видят график в публичном профиле `/t/[slug]` и знают, когда что ждать. Одна новелла может стоять на нескольких днях (Пн/Ср/Пт), с опциональным временем и короткой заметкой («1 глава», «платные главы»).

### Миграция 017 — `migrations/017_translator_schedule.sql`
- Таблица `translator_schedule (id, translator_id, novel_id, day_of_week, time_of_day, note, sort_order)`
- `UNIQUE (translator_id, novel_id, day_of_week)` — одну и ту же новеллу в один день не дублируем
- RLS: SELECT — все (анон в т.ч.), INSERT/UPDATE/DELETE — только владелец (свой `translator_id`), плюс override для админа

### Админка
- Новая страница `/admin/schedule` — сетка из 7 дней (Пн–Вс). В каждом дне — слоты с мини-обложкой, названием, временем/заметкой. Кнопка «× убрать» и «+ Добавить».
- Модалка `AddSlotModal`: быстрые пики из своих новелл + полнотекстовый поиск по всем новеллам + чип-выбор дней (multi-select) + опциональное время и заметка. Можно добавить одной отправкой сразу на Пн/Ср/Пт.
- Кнопка «📅 Расписание» в шапке `/admin`.

### Публичный профиль `/t/[slug]`
- Блок `TranslatorSchedule` — сетка 7 дней, сегодняшний подсвечен (рамка акцентом + бейдж «сегодня»). Клик по слоту ведёт на страницу новеллы.
- Если расписание пустое — блок не рендерится.
- Черновики/отклонённые новеллы в публичном расписании не показываются, даже если переводчик случайно их добавил.

### Константы
- `WEEKDAY_LABELS_SHORT`, `WEEKDAY_LABELS_LONG`, `WEEKDAYS` в `src/lib/admin.ts` — чтобы ISO-формат дней (0=Пн … 6=Вс) был одинаковым в админке и на публике.

## Итерация 24 — покупка платной главы: конфигурируемая цена + защищённый RPC + подсветка купленного

### Что было не так
1. Цена была прошита — `DEFAULT_CHAPTER_PRICE = 10` во всех местах, админка не задавала свою цену.
2. `buy_chapter(p_user, p_novel, p_chapter, p_price)` **принимал цену параметром** — клиент мог прислать `p_price=1`.
3. `chapter_purchases` без RLS — любой authenticated теоретически видел чужие покупки.
4. Платная глава для анонима не вставала в paywall — валилась мимо, грузила текст (баг).

### Миграция 018 — `migrations/018_chapter_price_and_security.sql`
- `chapters.price_coins int NOT NULL DEFAULT 10` + `CHECK (price_coins BETWEEN 1 AND 500)`
- `chapter_drafts.price_coins` — чтобы автосейв цену не терял
- Переписан `buy_chapter(p_novel, p_chapter)` → `jsonb`:
  - цена **берётся из БД**, клиент не посылает
  - возвращает `{ ok, error?, price?, balance?, already_owned? }`
  - ошибки: `not_authenticated`, `chapter_not_found`, `chapter_is_free`, `insufficient_balance`
  - идемпотентно: повторная покупка купленной главы → `ok: true, already_owned: true` без списания
  - атомарно: `FOR UPDATE` + `ON CONFLICT DO NOTHING`
- Старая сигнатура `buy_chapter(uuid, bigint, int, int)` удалена (`DROP FUNCTION`)
- RLS на `chapter_purchases`:
  - `purchases_self_read` — читатель видит свои
  - `purchases_translator_read` — переводчик видит покупки своих новелл (для аналитики)
  - `purchases_admin_all` — админ всё
  - `INSERT/UPDATE/DELETE` у `authenticated` отобраны — только через `buy_chapter` (SECURITY DEFINER)
- RPC `my_purchased_chapters(p_novel) → int[]` — для подсветки в списке глав

### UI
- `ChapterForm` — если включено «платная глава», появляется поле «Цена, монет» (1–500, default 10). Сохраняется и в `chapters`, и в `chapter_drafts`.
- `ChapterPaywall` — теперь вызывает новый RPC без `p_price`, парсит jsonb-ответ и показывает человеческие ошибки.
- Страница главы (`/novel/[id]/[chapterNum]`):
  - тянет `chapter.price_coins`, пробрасывает в paywall
  - **фикс бага:** анонимный читатель на платной главе теперь получает экран «войди → купи» с `next=`, а не текст главы
- Страница новеллы (`/novel/[id]`):
  - через `my_purchased_chapters` подтягивает Set купленных глав читателя
  - подсвечивает строку и плашку цены, меняет кнопку «Купить» → «Читать», ставит бейдж «✓ куплено»
  - показывает **реальную** цену главы (а не «10 монет»), с корректным склонением монета/монеты/монет

## Итерация 25 — пагинация глав + отложенная публикация + черновики

### Модель
Статусы главы больше не хранятся отдельной колонкой — их даёт уже существующий `published_at`:
- `NULL` — черновик (не виден никому кроме переводчика/админа)
- `> now()` — запланированная публикация (тоже только владельцу)
- `<= now()` — опубликована, видна всем

Так проще и не тянет за собой ни крон, ни триггеры: проверка — обычный предикат в каждом публичном запросе.

### Миграция 019 — `migrations/019_chapter_scheduling_and_drafts.sql`
- `ALTER COLUMN published_at DROP NOT NULL` (идемпотентно — через DO/BEGIN/EXCEPTION)
- Составной индекс `idx_chapters_novel_chnum (novel_id, chapter_number DESC)` — ускоряет пагинацию списка глав

### `ChapterForm` — режим публикации
Три радио-таба внизу формы:
1. **✓ Опубликовать сейчас** (по умолчанию) — `published_at = now()`
2. **⏰ Запланировать** — datetime-local, `min = now + 5 мин`; если ввели прошлое/ближайшую минуту, трактуем как «сейчас»
3. **📝 Сохранить как черновик** — `published_at = null`

`novels.latest_chapter_published_at` обновляется **только** при реальной публикации (now), черновики и запланированные дату не поднимают. При редактировании стартовый режим определяется по текущему `published_at`.

Кнопка submit меняет текст под режим («Опубликовать главу» / «Запланировать» / «Сохранить как черновик»).

### Страница новеллы `/novel/[id]`
- **Пагинация**: `?page=N`, 50 глав на страницу, нижний пагинатор «← Новее / Страница N из M / Старее →». Общий count берётся параллельно через `{ count: 'exact' }`. Первую главу для кнопки «Читать первую» тянем отдельным лёгким запросом, чтобы не зависеть от текущей страницы.
- **Фильтр статуса** для читателя: `published_at IS NOT NULL AND <= now()`. Переводчик / админ видит всё + бейджи «📝 черновик» и «⏰ выйдет через 2ч» + подсветка строки.
- Купленные по-прежнему подсвечиваются `✓ куплено`.

### Страница главы `/novel/[id]/[chapterNum]`
- Черновики и запланированные: `notFound()` для всех кроме владельца и админа.
- В режиме предпросмотра владелец видит жёлтый баннер «Это черновик» или «Запланировано на …».
- Prev/Next навигация для читателя берёт **ближайшую видимую** главу (не строго `num ± 1`), чтобы не уводить на черновик. Владельцу показываются соседние как есть.

### Лента `/feed`
- Добавил `.not('published_at', 'is', null).lte('published_at', now)` — будущие публикации в ленте не мелькают.

## Итерации 26–33 — автономная сессия

Алёна попросила прогнать оставшиеся недоделки пока её нет. Сделал 8 итераций подряд в одну ветку `claude/integrate-tene-chaptify-qMP0h` / PR #7 (батч).

### 26. Аудит мелких жалоб
Большинство уже закрыто в прошлых итерациях: `/feed` ссылки работают, `coin_balance` в шапке живёт в `SiteHeader`, `middleware.ts → proxy.ts` переименован, имя переводчика кликабельно в feed/novel/moderation/paywall. Fix: `BookmarkCard.tsx` — имя автора было span, теперь `<Link>` к `/t/<slug>` (подтянул translator_slug через отдельный запрос к `profiles`).

### 27. Уведомления переводчику о решении по заявке
Миграция 020: триггер `on_translator_application_reviewed` на UPDATE OF status. `pending → approved/rejected` пишет строку в `notifications` кандидату: approve — «Твоя заявка одобрена», rejected — с текстом `reviewer_note`. Добавил иконки для новых типов в `iconFor()`.

### 28. Fix: неработающая кнопка ♥ в NovelCard
Заглушка ♥ в обложке карточки не имела `onClick`. Управление полкой живёт на странице новеллы (`BookmarkButton`, который работает с iter 18). Убрал обманывающую заглушку — клик по карточке ведёт на страницу новеллы, там полноценный dropdown со статусами.

### 29. «Похожее» fallback: score-ранжирование
Было: если коллаборативная `get_similar_novels_by_readers` пуста — брали 6 новелл того же автора. Стало: пулл расширен (переводчик, автор, пересечение жанров) через три параллельных запроса, дедуп по id, ранжирование в JS:
- `+3` тот же переводчик
- `+1` за каждый общий жанр
- `+0.5` тот же автор, `+0.5` та же страна
- `-1` если кандидат 18+, а текущая не 18+

Тайбрейкер — `average_rating`. Фильтр `moderation_status=published` везде.

### 30. Inline-глоссарий в читалке
Термины из `novel_glossaries` теперь подсвечиваются прямо в тексте главы: после hydrate в `ReaderContent` TreeWalker проходит по текстовым узлам, оборачивает совпадения в `<span class="glossary-term">` (регулярка с `\p{L}\p{N}` границами для кириллицы, более длинные термины идут первыми чтобы не конфликтовали). Клик показывает поповер с переводом и категорией (Персонаж / Место / Термин / Техника / Прочее). Закрывается по клику вне или Esc. Страница главы параллельно с текстом подтягивает глоссарий новеллы.

### 31. `external_links` в NovelForm
Колонка jsonb была в схеме с миграции 004, UI не было. Добавил:
- `NovelFormValues.external_links: Array<{label, url}>` — динамический список со строками «название + URL + ×», кнопка «+ Добавить ссылку»
- Edit-страница пробрасывает в initial, толерантна к мусору в БД
- На странице новеллы (`/novel/[id]`) между `ReleasePace` и списком глав появился блок «Оригинал» — chip-ссылки с `target="_blank"`, fallback на hostname если лейбл пуст

### 32. Публичный тумблер цитат в профиле
Коллекция цитат уже работала (`QuoteCollection`). Добавил тумблер 🌐/🔒 на каждую — флажок `user_quotes.is_public` (из миграции 014). Публичные попадают в пул для `QuoteOfTheDay` на главной через уже готовую `random_public_quote()`. Оптимистичный апдейт с откатом на ошибке.

### 33. Поиск через pg_trgm
Было: `ilike %q%` без fuzzy и без фильтра черновиков.

Миграция 021:
- `CREATE EXTENSION pg_trgm`
- GIN-индексы `gin_trgm_ops` на `title/title_en/title_original/author`
- RPC `search_novels_trgm(q, lim)` ранжирует по `similarity()` (русский заголовок `×1.1`, автор `×0.9`), порог `> 0.12`, фильтр `moderation_status = 'published'`

`SearchClient` сначала пробует RPC; если миграция не накачена — fallback на ilike, но уже с фильтром published (в старой версии черновики могли светиться).

## Что я НЕ трогал в эту сессию

- **TipTap-редактор** — замена BB-редактора требует выбора пакета, стиля и рискует сломать глоссарий/автосейв/preview. Оставил на решение Алёне.
- **Массовый импорт глав** — уже есть `BulkChapterUpload` + страница `chapters/bulk`. Доработка требует понимания формата файлов, с которым работает переводчик.
- **CommentsSection** — в PROGRESS было «использует chapter_id и content/username». Проверил: схема уже правильная (`chapter_number`, `text`, `user_name`) — запись в PROGRESS устарела, скорее всего починено в iter 14.

## Миграции — накатить на Supabase в порядке

1. `001_roles_coins_subscriptions.sql` … `019_chapter_scheduling_and_drafts.sql` (как раньше)
2. **`020_translator_application_notifications.sql`** — триггер уведомления кандидата в переводчики
3. **`021_trgm_search.sql`** — расширение pg_trgm + индексы + RPC нечёткого поиска

После 001 — проверить что `user_name = 'alena'` в UPDATE в 001 совпадает с твоим реальным профилем.

## Итерации 34–40 — вторая автономная сессия

Алёна попросила доделать тёмную тему читалки + dashboard переводчика + доп-фичи «на моё усмотрение». 7 итераций, один PR с коммитами — все в ту же ветку.

### 34. Темы читалки (light / sepia / dark)
`ReaderSettings.theme` (persist в localStorage). CSS-переменные через `[data-theme]` на `.reader-wrapper`:
- **light** — кремовая бумага (как раньше, дефолт)
- **sepia** — тёплая состаренная бумага, меньше синего света
- **dark** — тёмно-серый фон + бежево-серый текст для ночи

Настройки читалки получили 3-up picker с tinted «Aa»-свотчами. Все reader-specific элементы (blockquote, chips, glossary underline, focus-mode opacity, затемнение картинок) подхватывают переменные. Без flash of wrong theme — wrapper не рендерится до hydration.

### 35. Dashboard переводчика: top-supporters + drop-off funnel
Миграция 022 — две RPC с проверкой доступа (только сам переводчик или админ):
- `translator_top_supporters(translator, since, limit)` — топ-N читателей по монетам за период
- `novel_reader_funnel(novel_id)` — агрегация `profiles.last_read` по главам, «где сейчас находятся читатели»

UI:
- `TopSupporters` — ранжированный список с accent→rose gradient-барами (длина ∝ total_coins), аватар, монеты, главы
- `NovelsTable` получила столбец 📉: модалка `FunnelModal` с bar-графиком drop-off, статистикой «медианная глава / дошли до последней», подсказкой про проседающие главы

### 36. Скачать EPUB
Колонка `novels.epub_path` лежала с миграции 008 без UI.
- API route `/api/novel/[id]/epub/route.ts`: полный URL → redirect как есть; относительный path → signed URL на 60с через `supabase.storage`. Sniff префикса (`epub/`, `covers/`, `chapter_content/`) чтобы translator мог положить файл в любой из бакетов.
- Кнопка «📘 EPUB» на странице новеллы рядом с `BookmarkButton`, если `epub_path` задан.
- `NovelForm` получил поле `epub_path` с подсказкой.

### 37. Уведомления о новой главе
Миграция 023: триггер на `chapters` (INSERT + UPDATE OF published_at). Момент «глава стала видимой» (прошла из NULL/future в ≤ now()) рассылает `new_chapter` всем активным подписчикам переводчика и всем, у кого новелла в `profiles.bookmarks`. UNION дедупит пересечение, переводчик себя исключает. `group_key='new_chapter:<novel_id>'` коллапсит массовую публикацию (50 глав) в одно уведомление per-новелла у каждого получателя через `list_notifications` DISTINCT ON.

### 38. Моя статистика в профиле
Новый блок `ReadingTotals` между stat-cards и `ReadingStreak`:
- Суммарно прочитано глав (∑ chapterId по `last_read`)
- Новелл в чтении
- ≈ часов с книгой (главы × 8 мин / 60)
- Любимый переводчик (тот, у кого больше всего прочитано)

Всё считается на сервере из `profiles.last_read` + уже подгружённой `readNovels` из `BookDiet`, плюс один доп-запрос к `profiles` за именем фаворит-переводчика. Блок не рендерится для новичков без чтения.

### 39. 🔥 На волне — trending по темпу глав
Блок на главной между `JournalStrip` и `QuoteOfTheDay`. Топ-6 новелл по числу `chapters.published_at` за последние 7 дней (threshold ≥2 чтобы одиночная публикация не выскакивала). Каждая карточка: ранг-плашка (#1..#6) с orange→red gradient, обложка с badge «+N», subtle огонёк-анимация в заголовке секции.

### 40. Поделиться цитатой
`QuoteBubble` рядом с «Сохранить цитату» получил «↗ Поделиться». Копирует в clipboard формат «цитата + атрибуция + прямой URL на главу», или открывает native Share Sheet на мобильных. `ReaderContent` пробрасывает `novelFirebaseId`/`novelTitle`.

## Новые миграции в этой сессии

- **`022_translator_analytics.sql`** — RPC `translator_top_supporters` + `novel_reader_funnel` (dashboard iter 35)
- **`023_new_chapter_notifications.sql`** — триггер-рассылка подписчикам и закладчикам (iter 37)

## Что я НЕ делал (из идей PROGRESS.md)

- **Читательские клубы** — требует отдельной схемы (комнаты, общий прогресс, модерация), это отдельная большая фича
- **TTS / аудио** — внешняя интеграция с платным API, нужен твой выбор провайдера
- **Real-time presence** — Supabase realtime каналы, требует клиентской подписки и тестов в живую
- **Push-уведомления** — service worker + браузерные permissions, сложный setup

## Итерации 41–46 — фиксы после CORS + новые фичи от Алёны

Шестой заход. После поправки `nginx.conf` (добавили `Accept-Profile, Content-Profile, Prefer` в `Access-Control-Allow-Headers` — остальное в конфиге не трогали) заработали админские SSR-запросы. Дальше — точечные фиксы и крупные фичи по списку.

### 41. Кликабельное имя переводчика в `NovelCard` (PR #8)
В iter 26 я только частично закрыл жалобу «клики по имени переводчика не работают» — починил `BookmarkCard`. В `NovelCard` имя оставалось plain text, потому что карточка сама была обёрнута в `<Link>`, а Next.js 16 запрещает вложенные `<Link>`.

Развернул: `NovelCard` теперь `<div>` с тремя независимыми ссылками — обложка + заголовок на новеллу, имя переводчика на `/t/{slug}`. Hover-лифт обложки сохранён через CSS-селектор на wrapper.

Хелпер `src/lib/translator.ts → fetchTranslatorSlugs(ids)` батчит запрос к `profiles`, отдаёт `Map<id, slug>` с fallback на `user_name`. Протащил через все места: `/`, `/catalog`, `/t/[slug]`, `/novel/[id]` (коллаборативная «Созвучие читателей» + score-fallback «Похожее»).

### 42. Фокус-режим в читалке работает по кнопке, без reload (PR #8)
`MutationObserver` на класс wrapper был ненадёжен — React при re-render иногда не давал observer'у сработать. Убрал его, добавил явную зависимость `settings.focusMode` в основном useEffect. Переключение сразу пересчитывает активный абзац и подсвечивает его, без F5.

### 43. Фильтр жанров и настроений в `/catalog` (PR #9)
Два молчаливых бага с кириллицей:
- `.contains('genres', [g])` хотел full superset на jsonb-массиве → никогда не матчил, каталог пустой
- mood-фильтр строил raw `.or('genres.cs.["фэнтези"]')` → Supabase не URL-энкодил кириллицу, и PostgREST эти записи молча игнорил

Заменил оба на `.overlaps()` — правильная семантика пересечения, кириллица корректно энкодится. Клик по жанру из карточки / жанровых чипов на главной + пикер настроения теперь возвращают реальные новеллы.

### 44. Внешний переводчик + «Это моя работа» (PR #9, миграция 024)
Переводчики добавляют работы друг друга (с разрешения). Если реальный переводчик потом регистрируется — может забрать новеллу себе через claim.

БД:
- `novels.external_translator_name / _url / _note` — заполняются когда `translator_id IS NULL`
- Пересобран `novels_view` с новыми колонками
- Таблица `novel_translator_claims (novel_id, claimant_id, proof, status, reviewer_*)` с RLS: claimant видит свои, админ — все
- RPC `request_novel_claim(novel, proof)` — залогиненный юзер может заклеймить только новеллу без зарегистрированного переводчика, дубли не проходят
- RPC `resolve_novel_claim(claim, approve, note)` — только админ; при approve `novels.translator_id` переписывается на claimant, external-поля чистятся
- Триггер: новая заявка → уведомление всем админам; решение → уведомление заявителю с `reviewer_note` при отказе

UI:
- `TranslatorPicker` (иксе, debounced 220 мс) — autocomplete по `profiles` с role translator/admin, «Это я» быстрая кнопка, если в поиске ничего → «+ Добавить <ввод> как внешнего переводчика» переключает на форму с name + url + **обязательной галкой «имею разрешение»**
- `NovelForm` получил обязательное поле «Переводчик *». Submit не даёт сохранить без переводчика или без галки согласия
- `/admin/novels/new` предвыбирает текущего пользователя; `/admin/novels/[id]/edit` подгружает существующего
- Страница новеллы показывает dashed-карточку «Внешний переводчик» с опц. внешней ссылкой + кнопкой «Это моя работа →» (клиентский `NovelClaimButton`) для залогиненных translator/admin
- Claim-модалка — показывает статус старых заявок (pending/approved/rejected с причиной отказа), поле для доказательств
- `/admin/moderation` — новая секция «Заявки "Это моя работа"» сверху над очередью модерации новелл с Approve / Reject-with-reason (компонент `ClaimCard`)

### 45. «Спасибо за главу» + чаевые переводчику (PR #9, миграция 025)
Под каждой главой — розовая карточка-призыв.

БД:
- `chapter_thanks (user_id, novel_id, chapter_number, translator_id, tip_coins)` с `UNIQUE(reader, chapter)`. RLS: свои + переводчик свои; direct-write у authenticated отобран, только через RPC
- RPC `thank_chapter(novel, chapter, tip_coins)` — валидирует 0–500, лочит баланс `FOR UPDATE`, списывает у читателя, зачисляет переводчику, пишет обе транзакции в `coin_transactions`, upsert в `chapter_thanks`. Идемпотентно для `tip=0`. Анти-злоупотребления: self-tip → 0, tip в новеллу без `translator_id` → 0
- RPC `chapter_thanks_summary(novel, chapter)` → `{ total_count, total_coins, my_thanked }` для UI
- Триггер: только денежные tips шлют `chapter_tip`-уведомление переводчику (лайки не спамят)

UI: бесплатное «♥ Спасибо» (становится «✓ Уже поблагодарил(а)»), кнопка-тоггл «💝 Чаевые» с пресетами +1 / +2 / +5 / +10 и полем свой суммы 1–500. Ошибки `insufficient_balance` / `not_authenticated` переведены в человеческий текст.

### 46. EPUB по уровню доступа, на лету (PR #9)
Вместо фонового скрипта, как первоначально обсуждали — каждый запрос `/api/novel/[id]/epub` собирает EPUB на сервере с учётом прав текущего читателя. Готовый файл из `novels.epub_path` остаётся как fallback (если заполнен).

Тиры:
- аноним / нет подписки и нет покупок → только `is_paid = false`
- активная `subscriptions` на переводчика → все опубликованные главы
- есть `chapter_purchases` → бесплатные + купленные
- владелец / админ → всё (preview)

Пустой доступ → 403 с подсказкой «купи / подпишись».

Имплементация: добавил `jszip` в зависимости. Новый `src/lib/epub.ts` собирает минимальный EPUB 2.0 (`mimetype` STORE + `META-INF/container.xml` + `OEBPS/content.opf` + `OEBPS/toc.ncx` + `OEBPS/style.css` + по `chN.xhtml` на главу + опциональная обложка). Содержимое глав тянется из `chapter_content` bucket батчами по 8. Имя файла — безопасный UTF-8 slug; заголовок `X-Epub-Tier` для отладки.

В `NovelForm` поле `epub_path` переименовано на «EPUB вручную (необязательно)» — подсказка объясняет, что пусто = сервер собирает сам.

## Миграции — новые в этой сессии

- **`024_external_translator_and_claims.sql`** — external_translator_* колонки + пересборка `novels_view` + таблица `novel_translator_claims` + RPC request/resolve + триггер уведомлений
- **`025_chapter_thanks_and_tips.sql`** — таблица `chapter_thanks` + RPC thank_chapter / chapter_thanks_summary + триггер уведомлений переводчику на денежные tips

## Что обязательно ставить после pull

```
npm install        # добавилась jszip (для EPUB)
```

И накатить на Supabase **в порядке**: `024_external_translator_and_claims.sql`, потом `025_chapter_thanks_and_tips.sql`.

## Что конфигурировали в nginx.conf

Единственное изменение: в `Access-Control-Allow-Headers` (оба `add_header` — в `if ($request_method = 'OPTIONS')` и снаружи) добавили к списку:
```
,Accept-Profile,Content-Profile,Prefer
```

Нужно для `@supabase/ssr` в Next.js 16. На `tene.fun` не влияет (tene-клиент эти заголовки не шлёт). После `nginx -s reload` админские карточки на chaptify начали нормально подтягивать данные.

## Предстоит

### Миграции — накатить на Supabase в порядке
1. `001_roles_coins_subscriptions.sql`
2. `002_catalog_extensions.sql`
3. `003_quotes_recommendations.sql`
4. `004_moderation_and_drafts.sql`
5. `005_reading_days.sql`

После 001 — проверить что `user_name = 'alena'` в UPDATE в 001 совпадает с твоим реальным профилем.

### Supabase Storage bucket
Для загрузки обложек и глав через UI должны существовать бакеты `covers` и `chapter_content` с настроенными политиками:
- `covers` — public read, INSERT для authenticated
- `chapter_content` — public read (или только для проверенных, но нам пока ок), INSERT/UPDATE для authenticated

### Известные недоделки
- **Поиск** в шапке не работает (нужен `pg_trgm` или отдельный индекс)
- **`/feed`** (лента обновлений) — ссылки ведут в никуда
- **Страница переводчика `/t/alena`** — клики по имени переводчика не работают
- **`coin_balance` в шапке** — после применения 001 вывести бейдж-монетку в `SiteHeader`
- **Коллекция цитат в профиле** — UI ещё нет, таблица уже есть
- **Middleware → Proxy** — Next.js 16 объявил `middleware` устаревшим, переименовать `src/middleware.ts` → `src/proxy.ts`
- **Кнопка «♥ В закладки»** на странице новеллы не работает (нужен клиентский хендлер + запись в `profiles.bookmarks`)
- **«Похожее» fallback** — ранжирование по score как в tene; сейчас просто по автору
- **Пагинация глав на странице новеллы** — когда >100 глав, грузятся все
- **Покупка платной главы** — RPC `buy_chapter` уже есть, но UI кнопки «Купить» ведёт в никуда
- **Комментарии** — текущий `CommentsSection` использует `chapter_id` и поле `content`/`profiles.username`, которые в реальной схеме называются иначе (`chapter_number` + `text` + `profiles.user_name`). Требуется привести к реальной схеме tene.
- **Глоссарий в читалке** — термины определены, но читатель их пока не видит в тексте главы (подсветка есть только в админке в preview). Сделать inline-tooltip при клике на термин.
- **Модерация новелл** — колонка `moderation_status` есть, но в форме пока всё сразу `published`. Нужен флоу «создать как draft → отправить на модерацию → админ ревьюит».
- **TipTap-редактор** для описания новеллы и текста главы — сейчас HTML-textarea с примитивным toolbar.
- **Массовый импорт глав** — загрузка `.zip` с файлами глав разом.
- **Ссылки на оригинал** (`external_links`) — колонка есть, UI в NovelForm нет.
- **Уведомление переводчика** об одобрении/отказе заявки — сейчас только UI-индикация. Надо инсертить в `notifications` таблицу.

### Будущие идеи
- Real-time «кто сейчас читает эту новеллу» через Supabase realtime presence
- Читательские клубы с общим прогрессом
- Аудиоозвучка главы через TTS
- Dark mode для читалки (тёплый серый на очень тёмном)
- Dashboard переводчика со статистикой просмотров/покупок

## Итерация 47 — фиксы 23.04.2026: 404 на профилях, поиск, Boosty, чаптеры

Работа по жалобам Алёны после боевого запуска:

### Корень проблем — RLS на `profiles`
На `public.profiles` стоит legacy-политика «`SELECT TO authenticated USING (auth.uid() = id)`» (с tene). Никакого public-read нет ни для anon, ни для остальных authenticated. Из-за этого ломалась куча страниц:

- `/t/[slug]` и `/u/[id]` всегда 404 (lookup сторонего профиля → пусто)
- В блоке «Над новеллой работают» появлялась строка-призрак «Переводчик» без имени (LEFT JOIN внутри view возвращает данные, но если ряд `novel_translators` orphan — display пустой)
- `/admin/subscribers` — клик по подписчику → 404 (тот же lookup)
- `/profile` карточка «Подписки» висела на «—» (запрос за переводчиками подписок проваливался)
- `fetchTranslatorSlugs` молча возвращал пустую Map → имена переводчиков на карточках не были ссылками

#### Решение — миграция 040
- Создан VIEW `public.public_profiles` с безопасным набором колонок: id, user_name, translator_slug, translator_display_name, translator_avatar_url, avatar_url, translator_about, payout_boosty_url, payout_tribute_channel, quiet_until, quiet_note, role, is_admin
- `last_read` и `bookmarks` отдаются ТОЛЬКО если в `settings.show_reading_publicly = true` (приватность из `/profile/settings`)
- `GRANT SELECT TO anon, authenticated` на view; RLS на `profiles` не трогается → tene-fronend по-прежнему читает свой ряд напрямую
- Бэкфилл-чистка: `DELETE FROM novel_translators WHERE user_id NOT IN profiles` — удаляет orphan-строки от тестов / удалённых аккаунтов
- Повторный INSERT на каждую новеллу с translator_id (страховка на случай если триггер 039 не отработал)

В коде заменены прямые запросы к `profiles` на `public_profiles` в:
- `src/app/t/[slug]/page.tsx` — все три варианта lookup
- `src/app/u/[id]/page.tsx` — by id и by user_name
- `src/lib/translator.ts::fetchTranslatorSlugs`
- `src/app/profile/page.tsx` — favorite translator, активные подписки + переводчики
- `src/app/profile/subscriptions/page.tsx` — список переводчиков-подписок
- `src/app/admin/subscribers/page.tsx` — имена активных подписчиков

### Subscriber-side: «Я подписан_а на» в /profile
- Карточка «Подписки» теперь показывает РЕАЛЬНОЕ число активных подписок (раньше всегда «—»)
- Под stat-cards новый блок: первые 3 переводчика с аватаром, именем и счётчиком «ещё N дн.»
- Полный список — на `/profile/subscriptions`

### Notification text «BOOSTY external_claim»
- В `/profile/subscriptions` функция `planLabel('external_claim')` возвращала строку как есть
- `providerLabel` не знал `vk_donut`, `patreon`, `other`
- CSS `.subs-provider` имел `text-transform: uppercase` — превращал «Boosty» в «BOOSTY»

Фиксы:
- `planLabel`: добавлен case `'external_claim' → 'Внешняя подписка'`
- `providerLabel`: добавлены `vk_donut`, `patreon`, `other`
- `.subs-provider`: убран uppercase, заменён на чип с акцентным фоном

### Boosty-warning в /admin/subscribers даже если ссылка указана
Переводчик настроил Boosty через новый редактор «Способы оплаты» (`translator_payment_methods`, мигр. 037), но страница смотрела ТОЛЬКО на legacy-поле `profiles.payout_boosty_url`. Жёлтая плашка «У тебя не указана ссылка на Boosty» висела навсегда.

Фикс: после проверки legacy-поля — fallback-запрос в `translator_payment_methods` (provider='boosty', enabled=true). Если есть запись — Boosty считается настроенным.

### Кнопка «Отозвать» подписку — миграция 041
Раньше у переводчика не было способа отозвать подписку у читателя, который слетел с Boosty. Теперь:
- RPC `revoke_subscription(p_subscription_id, p_reason)` — ставит status='cancelled', expires_at=now(), отправляет push с причиной
- В `SubscribersClient` под каждой активной подпиской — кнопка «⊗ Отозвать» с prompt причины
- При `subscriptions_status_check` без 'cancelled' — миграция расширяет CHECK

### Поиск по новостям и переводчикам
В `/search` блок результатов разделён на 4 секции с бейджем-источником:
- **Переводчики** (профили) — поиск по `public_profiles` фильтрует по role translator/admin, ищет по user_name / display_name / slug / about
- **Новеллы** (каталог) — как раньше, через `search_novels_trgm` или ilike
- **Новости** (журнал) — поиск в `news_posts` по title / subtitle / body, только опубликованные
- **Термины и персонажи** (глоссарии) — как раньше

Каждая секция получила бейдж типа `<span class="search-source-badge">профили</span>` рядом с заголовком.

### Чаптер-row click не работал
В списке глав на странице новеллы overlay-link имел z-index:0, а дочерние блоки — z-index:1 с position:relative. В итоге клик по строке всегда попадал в текстовый дочерний блок и не доходил до Link.

Фикс в CSS `.chapter-item`:
- `.chapter-item-overlay` → z-index:1
- `.chapter-item > *:not(.chapter-item-overlay)` → `pointer-events: none`
- `.chapter-item .btn, .chapter-item button` → `pointer-events: auto; z-index:2`

Теперь клик по любому месту строки открывает главу. Кнопки «Править» и «Читать» работают точечно, как раньше.

### NovelCredits — пропускаем orphan
Если в `novel_credits` view приходит ряд без display_name и user_name (orphan от удалённого профиля) — теперь не рендерим. Раньше показывался ряд «Переводчик» без ссылки. Миграция 040 ещё и из БД такие удаляет, но клиент подстрахован.

## Платёжная система end-to-end (актуально на 23.04.2026)

### Базовая модель: chaptify не проводит деньги через себя
Деньги читателя → переводчику напрямую через **Boosty / Tribute / VK Donut / Patreon**. Chaptify только подтверждает факт платежа, чтобы открыть платные главы.

Что у нас «своё» — внутренние **монеты**: цифровая услуга от платформы (продаются за рубли), тратятся на покупку отдельных глав или чаевые. Это не выплаты другим людям — налоговая упрощается.

### Со стороны переводчика

#### 1. Подключить способ оплаты — `/profile/settings` → блок «Способы оплаты»
Таблица `translator_payment_methods` (мигр. 037). Можно подключить несколько провайдеров. Каждый ряд:
- provider: `boosty | tribute | vk_donut | patreon | other`
- url: ссылка на свою страницу оплаты
- instructions: для читателей («оплати тир Фанат и напиши мне в ЛС»)
- enabled: вкл/выкл
- tg_chat_id: ТОЛЬКО для Boosty — id закрытого TG-чата подписчиков для авто-сверки (мигр. 038)

Старое поле `profiles.payout_boosty_url` (settings) ещё работает (бэкфилл из него попадает в `translator_payment_methods` при первой миграции).

#### 2. Tribute — webhook-автомат
Есть честный webhook от Tribute. На `/admin/payouts` копируется уникальный URL `https://tene.fun/webhook/tribute/<token>`, вставляется в Tribute. Каждый платёж → бот находит читателя по `payment_code` или `telegram_id` → начисляет монеты или активирует подписку (`provider=tribute`).

Health-индикатор «активен N мин назад» по `payout_last_tribute_event_at`.

#### 3. Boosty — три механики (по убыванию автоматизации)

**A. Полный авто-синк через TG-чат (мигр. 038)**
1. Переводчик настраивает Boosty так, что подписчики автоматически попадают в его закрытый TG-чат
2. В этот же чат приглашает `@chaptifybot` участником
3. На странице оплаты у читателя кнопка **«Я уже подписан(а) — открыть автоматически»**
4. Бот через `getChatMember(chat_id, user_tg_id)` проверяет членство → RPC `grant_subscription_from_boosty_chat` → подписка active на 1 месяц

Если читатель отписывается на Boosty → автоматически вылетает из чата → следующая попытка авто-синка не сработает. Существующая подписка живёт до `expires_at`, потом сама expired (см. ниже про отмены).

**B. Полу-авто claim через бота (PR #5 в server-репо)**
Читатель платит на Boosty. На странице оплаты получает короткий код `C-XXXXXXXX`. Идёт в `@chaptifybot`, шлёт `/claim <код>` → переводчик в личку видит inline-кнопку «Одобрить / Отклонить».

**C. Ручной claim через UI (мигр. 035)**
Если бот недоступен / читатель не в TG: на `/t/[slug]` оформляет заявку через форму. Заявка попадает в `subscription_claims` → переводчик видит на `/admin/subscribers` → одобряет / отклоняет → создаётся `subscriptions` с `plan='external_claim'`, `provider='boosty'`.

#### 4. Управление подпиской переводчиком — `/admin/subscribers`
- **Заявки** — pending claims с кнопками Одобрить / Отклонить (с причиной) / Написать в ЛС
- **Активные подписчики** — все active subscriptions с expires_at; кнопка **⊗ Отозвать** (мигр. 041) для случая «оплатил, потом отписался на Boosty», даёт причину в push читателю
- **История** — последние 30 разобранных заявок

#### 5. Учёт выплат — `/admin/payouts` + `/admin/all-payouts` (только админ)
- payout_cycles (мигр. 011): монеты, заработанные переводчиком, → ₽ → выплачены такого-то числа таким-то способом
- Дашборд /admin/all-payouts: селектор месяца, настраиваемый курс и комиссия, CSV-экспорт, кнопка «Выплачено ✓» по каждой строке

### Со стороны читателя

#### 1. Найти платный контент
- На странице новеллы рядом с главой — плашка с ценой в монетах: «10 монет», «✓ куплено», «✓ команда», «✓ подписка»
- В каталоге у новеллы значок 💝 если у переводчика подключены способы оплаты

#### 2. Платный paywall (`ChapterPaywall`)
Открывается, когда `chapter.is_paid=true` и нет ни подписки, ни покупки.

Опции:
- **Купить за N монет** — RPC `buy_chapter` (мигр. 018), цена из БД, не из клиента; если не хватает — кнопка «Пополнить баланс»
- **Подписаться на переводчика** — список всех его `translator_payment_methods` с инструкцией; если есть Boosty TG-чат → кнопка «Я уже подписан(а)» (авто-синк); кнопка «Заявить подписку» (claim-flow)

#### 3. Пополнение монет — `/profile/topup`
Сейчас: пакеты 100 / 500 / 1000 / 2500 монет, выбор Tribute / Boosty, ввод payment_code в коммент платежа.

Tribute → webhook автоматический. Boosty → ручная сверка переводчиком (или Алёной).

#### 4. Чаевые после главы (мигр. 025)
- Кнопка «♥ Спасибо» (бесплатно, ставит флажок благодарности)
- Кнопка «💝 Чаевые» — пресеты +1 / +2 / +5 / +10 монет, RPC `thank_chapter` атомарно списывает с читателя, зачисляет переводчику, шлёт push

#### 5. Список своих подписок — `/profile` + `/profile/subscriptions`
- На главной `/profile`: счётчик в stat-card + strip с первыми 3 переводчиками
- На `/profile/subscriptions`: три секции (Активные / Ожидают подтверждения / История) с переводчиком, провайдером, planLabel, expires_at, предупреждение «осталось 5 дней» если <7

### Что происходит после отмены на Boosty?

**Короткий ответ: подписка живёт до `expires_at` и потом естественно истекает.**

Подробнее:
- При одобрении claim или авто-синке `expires_at = now() + N месяцев` (по умолчанию 1)
- Chaptify **не видит** факта отмены на Boosty (нет API)
- Читатель пользуется подпиской до её истечения — это честно, он за этот период заплатил
- После `expires_at` `can_read_chapter` начнёт выдавать `false` для платных глав
- Если переводчик хочет отозвать подписку досрочно (например, узнал про cancel из личных сообщений Boosty) — кнопка **⊗ Отозвать** на `/admin/subscribers` (мигр. 041)
- Авто-синк через TG-чат (вариант A) НЕ ре-проверяет существующие подписки. Делает grant только когда читатель сам нажимает «Я уже подписан(а)». Если читатель не нажимает — мы не узнаём что он отписался. Можно добавить периодический cron (бот раз в день проверяет всех active boosty-подписчиков), но пока не делаем — слишком много getChatMember-вызовов.

### Уведомления о подписках
- `subscription_claim` — переводчику когда читатель отправил claim
- `subscription_approved` — читателю когда одобрили
- `subscription_declined` — читателю когда отклонили (с причиной)
- `subscription_revoked` — читателю когда переводчик отозвал (с причиной)
- `chapter_tip` — переводчику когда читатель отправил денежные чаевые

Все идут через таблицу `notifications` + триггеры → видны в `/notifications` + `@chaptifybot` рассылает важные.

### Tene-safety
- Никакая логика tene не переписана — `can_read_chapter` остался прежним, `*_chaptify` версия рядом (мигр. 036)
- Все новые таблицы / view с явными `GRANT TO anon/authenticated` без переписывания исходных RLS

## Итерация 48 — аудит платёжки + 4 нишевых фичи

Провела сквозной аудит платёжного флоу (фронт + сервер + бот + схема БД). Ниже — критичные дыры, которые закрыла в этом же коммите, плюс четыре новых блока в стилистике сайта.

### Платёжный аудит — исправленное

1. **Бот: `_do_approve()` hardcoded `provider='boosty'`** (`server/chaptify-bot/bot.py:427`)
   Когда переводчик одобрял claim через `/approve` бота, запись в `subscriptions` всегда получала `provider='boosty'`, даже если заявка пришла для Tribute / VK / Patreon. На /profile/subscriptions и в триггерах уведомлений это путало. Теперь берём `claim.provider` с валидацией.

2. **`PROVIDER_LABEL` в `SubscribersClient.tsx`** — не было `patreon` и `other`. Для этих провайдеров в UI светилась raw-строка. Добавлено.

3. **ChapterPaywall не получал `tg_chat_id`** из запроса к `translator_payment_methods` в `/novel/[id]/[chapterNum]/page.tsx`. Из-за этого кнопка «⚡ Я уже подписан» для Boosty никогда не появлялась (`autoSyncAvailable=false`). Добавила `tg_chat_id` в select.

4. **Фильтр провайдеров в `/admin/subscribers`**: когда у переводчика подписчики из нескольких платформ, раньше был общий список. Теперь над списком — табы (Все / Boosty / Tribute / …) со счётчиками.

### Оставшиеся дыры в платёжке (не критично, нужен внешний setup)

- **Tribute webhook**: `payout_tribute_webhook_token` есть (мигр. 010), `tribute_chaptify_webhook.py` упомянут в PROGRESS, но на сервере его нет. Без него автоматическое пополнение монет Tribute не работает — только ручная сверка переводчиком.
- **VK Donut / Patreon**: объявлены в `translator_payment_methods.provider` CHECK, но никакой автомат-интеграции. Работают только через claim-flow.
- **`grant_subscription_from_boosty_chat` tier hardcoded 1 месяц** — параметр есть, но `auth-service-chaptify/index.js` шлёт константу. Для начала норм, потом можно читать тир из payment_method.

Эти три пункта требуют либо живого обращения к API платформ, либо действий в их dashboard — оставляю тебе на ручную работу, когда будет время.

### Новые фичи (по твоему списку)

#### 1. «Моя история с этой новеллой» (читателю на /novel/[id])
Персональный таймлайн-блок на странице новеллы:
- «Читаешь с [5 мес. назад]» (из `last_read.timestamp`)
- N / M глав (где я сейчас / всего)
- N сохранённых цитат (из `user_quotes`)
- N «спасибо» под главами (из `chapter_thanks`)
- «Дней с этой книгой» — приблизительно от старта до сегодня
- Progress-bar в стилистике каталога, с градиентом gold → accent
- Когда новелла завершена и читатель дочитал — заголовок меняется на «Эта новелла сопровождала тебя»

Рендерится только для залогиненных, только если есть хоть какая-то история. Файлы:
- `src/components/novel/MyNovelHistory.tsx`
- data-fetch в `src/app/novel/[id]/page.tsx`

#### 2. Фильмография на /t/[slug] (IMDb-style)
Блок «Где ещё помогал_а» под расписанием. Показывает все участия переводчика в команде — редактор, корректор, иллюстратор, бета. Если переводчик только «главный» (а в команде у других нет) — блок не рендерится, чтобы не дублировать «Все новеллы» сверху.

- Чипы сверху: сводка по ролям («Редактор · 4», «Бета · 2»)
- Строка на каждое участие: мини-обложка, название, роль, % доли, заметка
- Данные — из `novel_translators` + `novels_view` (где `translator_id != profile.id` или `role != 'translator'`)

Файлы:
- `src/components/translator/TranslatorFilmography.tsx`
- добавлен fetch + рендер в `src/app/t/[slug]/page.tsx`

#### 3. Звезда недели на главной
Витрина-карточка между TrendingNovels и QuoteOfTheDay. Выбирается математически по активности за последние 7 дней:
- +10 баллов за каждого нового подписчика
- +2 балла за каждую новую главу
- +0.05 за каждую монету (чтобы топап от 1000 монет дал 50 баллов)

Если за неделю score никого не поднял — блок не рендерится (ничего не показываем — пусто лучше фейка).

Дизайн: мягкий градиент (#fff6e4 → accent-wash), серифный заголовок с именем-ссылкой, три счётчика, портрет справа с пульсирующей звёздочкой. Тёплый «читальный» стиль, без GAIM-эстетики.

Файлы:
- миграция `042_star_of_the_week.sql` (RPC `star_translator_of_the_week()`)
- `src/components/home/StarOfTheWeek.tsx`
- подключение в `src/app/page.tsx`

#### 4. «Когда читают твоих» (переводчику на /admin/analytics)
Heatmap 7×24 (дни недели × часы). Нижний уровень — бежевый, пиковые слоты — accent-цвет. Над сеткой — чипы с топ-3 слотами: «Вт 20:00 · 47», «★ Пт 21:00 · 53» и т.п. Подсказка на ховере ячейки: «Вт 14:00 · 23 открытия».

Временная зона: сервер агрегирует в UTC, клиент делает сдвиг в локаль браузера при рендере. Это честно — автор в Мск видит свои 9:00 как 9:00, а не UTC.

Файлы:
- миграция `043_hourly_reader_heatmap.sql` (RPC `translator_hourly_heatmap(translator, days)`, SECURITY DEFINER + own/admin check)
- `src/components/analytics/HourlyHeatmap.tsx`
- подключение в `src/app/admin/analytics/page.tsx`

### Миграции — накатить по порядку

```
042_star_of_the_week.sql      # RPC для главной витрины
043_hourly_reader_heatmap.sql # RPC тепловой карты
```

Без них соответствующие блоки просто не появятся (try/catch в коде) — tene не пострадает.

### Что осталось в твоём большом списке (отложено)

Эти фичи требуют либо крупной работы над платёжкой, либо внешних API — имею смысл делать, когда будет живая обратная связь с переводчиками:

- **Утренний дайджест** — требует scheduled job в `@chaptifybot`, дизайн сообщения, опций отключения. Отдельной итерацией.
- **Встроенная подписка Patreon-lite** — нужен настоящий split-payment через провайдера (Stripe Connect / ЮKassa). Без этого chaptify всё ещё «без денег сквозь нас».
- **Storyfront (landing per-новелла)** — отдельный маршрут `/novel/[id]/share` с UTM-трекингом. Пока не делала.
- **Стрик-дневник** — хорошая идея, но большой кусок работы: журнал + emoji + «заморозки» + ежемесячный wrap. Делаем отдельно.
- **Crowdfund / Bounty** — денежные механики, без платёжного платформенного счёта рано.
- **Кросспостинг в Boosty / VK** — нужны их API / токены пользователей.
- **Академия переводчика** — это контентная вертикаль, отдельный проект.

Часть уже сделана раньше: co-translation (мигр. 034), marketplace (032/033), chapter-thanks с сообщением (025), тихий режим (037), публичный роадмап (031).

## Итерация 49 — per-translator монеты (новая платёжная модель)

Переход от «общего платформенного кошелька» к per-translator кошелькам.
Юридическая основа: chaptify деньги между людьми <b>не проводит</b>,
каждый переводчик принимает оплату сам и платит налоги сам (самозанятость).
Это снимает статус финпосредника с платформы и делает схему законной
для одиночного самозанятого владельца.

### Миграция 045

**Новая таблица `reader_translator_coins`**
Ключ (user_id, translator_id), balance int. Один читатель может иметь
разные балансы у разных переводчиков. RLS: читатель и переводчик видят
свои строки, админ — всё. Прямые writes запрещены, всё через SECURITY
DEFINER RPC.

**Расширение `subscription_claims`**
Добавлены колонки `kind` ('subscription' | 'coins') и `coins_amount`. Один
стол для обоих типов заявок — переводчик видит их вместе в
/admin/subscribers с бейджем-типом.

**Новые / переписанные RPC**
- `submit_coins_claim(translator, provider, amount, external, note)` —
  заявка на покупку монет у конкретного переводчика. Генерит код `M-XXXX`.
- `approve_subscription_claim(id)` — ветвится по kind: создаёт подписку
  ИЛИ зачисляет монеты в reader_translator_coins.
- `buy_chapter` переписан — списывает из per-translator кошелька, не
  зачисляет переводчику (деньги уже у него за пределами chaptify).
  Ошибка `external_translator` если у новеллы нет translator_id.
- `thank_chapter` (обе сигнатуры — 3-арная и 4-арная) — теперь чистое
  социальное «♥», ignore-ит tip_coins и p_message, сохраняет только
  факт благодарности.
- `my_translator_wallets()` — список всех моих кошельков для /profile.
- `my_balance_with(translator_id)` — баланс у одного переводчика
  для paywall и /t/[slug].

**Старое**
- `profiles.coin_balance` НЕ трогаем — tene использует. В новой модели
  просто не обновляется. Мигрировать старые значения в новые кошельки
  не стали — слишком много ambiguity, кого что пополнил.
- Триггер `on_chapter_tip` удалён — он срабатывал только при tip>0,
  теперь tip всегда 0.

### UI

**Новое**
- `/t/[slug]` — блок <TranslatorWallet>: показывает баланс читателя у
  этого переводчика + форму «Пополнить» (amount chips + choose provider +
  свой ник + опциональный коммент). После submit — показывает код M-XXXX
  и инструкцию «переведи на его Boosty с этим кодом в комментарии».
- `/profile` — stat-card «Монеты» показывает сумму по всем кошелькам +
  «у N переводчиков», блок «Мои монеты по переводчикам» со списком
  кошельков.
- `/profile/topup` — полностью переделана: больше не продаёт «общие»
  монеты. Гайд «как это работает» + список твоих кошельков → ссылка
  на `/t/[slug]`.
- `/admin/subscribers` — каждая заявка получает бейдж «📅 подписка» или
  «💰 монеты» + описание «покупка 300 монет» вместо «на 1 мес.». Hint
  для coins-заявок объясняет: «одобрение зачислит монеты на per-translator
  кошелёк».

**Упрощено**
- `ChapterThanks` — убран весь UI чаевых (пресеты +1/+2/+5, свой ввод,
  textarea для сообщения). Остался только бесплатный ♥. После твоей
  просьбы «зачем чаевые переводчику? смысл в них? — убрать». Монеты
  переводчик получает через подписки / покупку монет, а не через
  «сжигание» на сайте.
- `ChapterPaywall` — показывает баланс «у этого переводчика» вместо
  «на счету», кнопка «Пополнить у Маши» ведёт на /t/[slug] а не на
  /profile/topup. Обработка новой ошибки `external_translator`.

### /help обновлён

Переписаны разделы:
- «Как работают монеты?» — объяснена per-translator модель
- «В чём разница между подпиской и монетами» — обновлено
- Новый вопрос «Почему монеты отдельные у каждого переводчика?» —
  объясняет юр-логику через аналогию с подарочными картами
- Для переводчиков: «Как устроены монеты и как я получаю деньги» —
  полностью переписано под новую модель («chaptify деньги не проводит»,
  «налоги платишь ты сам», ссылка на 422-ФЗ)
- «Зачем вообще монеты» — микро-платёж / экономия комиссий / одна
  ФНС-запись вместо пятидесяти
- «Когда приходят деньги» — теперь «сразу», без админского месячного
  цикла

### Tene-safety

- profiles.coin_balance не трогается
- Все RPC SECURITY DEFINER — tene не зовёт их
- subscription_claims уже создана в tene — мы просто добавили колонки

### Что дальше

Осталось (отдельные итерации):
- Выпилить `/admin/all-payouts` и `/admin/payouts` или переосмыслить
  как «мой личный доход» (справочный дашборд без «выплат»)
- Storyfront landing per-новелла с UTM
- Утренний дайджест (нужен scheduled job в @chaptifybot)
- Обучающий экран «как это работает» при первом посещении /t/[slug]
  или при первой платной главе — чтобы не пугать читателя словом «код»

## Итерация 50 — справка переписана, /admin/payouts выпилены

Подгонка UI-текстов и лишних страниц под новую модель.

### /help полностью переделан

- Контакт-бот везде заменён: `@tenebrisverbot` → `@chaptifybot`
- Для читателей: пошаговый гайд «Как пополнить кошелёк у переводчика»
  (с кодом M-XXXX), новый вопрос «Когда выгоднее подписка, а когда монеты»
- «Я оплатил, но монеты не пришли» — переписан: теперь объясняет, что
  зачисление делает переводчик в /admin/subscribers
- «Возврат» — переводчик решает сам, упомянута кнопка «⊗ Отозвать»
- Для переводчиков новые разделы:
  - «Как одобрять заявки» (включая команду `/claim` через бота)
  - «Как отозвать подписку, если читатель слетел с Boosty»
  - «Тихий режим», «Расписание публикаций», «Где моя статистика»
  - «Как быть с налогами — я самозанятый_ая» (422-ФЗ, НПД, Boosty-ФНС
    интеграция, «chaptify не выдаёт чеков вместо тебя»)
- Старый раздел про Tribute webhook → переписан про
  /profile/settings → «Способы оплаты» (новый редактор)
- /pay-methods — теперь описывает все 5 провайдеров
- /change-name — отсылает на /profile/settings (был стаб «через бота»)

### Выпилены легаси-страницы

- `/admin/payouts` → редирект на `/profile/settings`. Раньше там был
  Tribute-webhook-токен и индикатор здоровья — всё это заменилось
  на PaymentMethodsEditor в настройках профиля. Файл
  `PayoutsClient.tsx` удалён.
- `/admin/all-payouts` → объяснительная страница «дашборд устарел».
  CSV-экспорт и расчёт «кому сколько доплатить» удалены — в новой
  модели мы никому не платим. `AllPayoutsClient.tsx` удалён.
- `/admin/analytics`: блок «К выплате (накоплено)» переосмыслен в
  справочный «Твои продажи за период» без фразы «админ переводит
  раз в месяц».

### Навигация

- `/admin` шапка: кнопка «💳 Настройки выплат» → «💳 Способы оплаты»
  (ведёт на /profile/settings); админская кнопка «💰 Выплаты
  переводчикам» удалена из admin-навигации.
- `SettingsForm` legacy-поле `payout_boosty_url` — подсказка теперь
  объясняет, что рекомендуется блок «Способы оплаты» ниже (не отсылка
  на устаревший /admin/payouts).

Без новых миграций — всё текстово / UI-правки. RPC
`translator_earnings_pending` пока остался (он читает старые покупки
глав, безобиден) — удалять не стали.

## Итерация 51 — мобильная адаптация (5 фаз, коммиты 99c4a1b…ec0919b)

Tene адаптирован под мобильные, chaptify был нет. Здесь — базовый
проход под iPhone / Android. Цель: 360–430 px всё выглядит адекватно,
тап-таргеты ≥ 44 px, iOS safe-area учитывается.

### Фаза 1 (99c4a1b) — фундамент

- `src/app/layout.tsx`: `export const viewport: Viewport` с
  `viewportFit: 'cover'`, `themeColor: '#F5EFE6'`. Это включает
  `env(safe-area-inset-*)` на iPhone X+. `userScalable` оставили
  дефолтно true — не отбираем pinch-zoom у людей со слабым зрением.
- `globals.css` html/body: `overflow-x:hidden`,
  `-webkit-tap-highlight-color: transparent`, `touch-action: manipulation`.
- `.container` на ≤540 px: padding 0 24 → 14 px (остаётся 332 px на
  360-м экране).
- `.btn`: height 40 → 44 px (Apple HIG).
- **Новый** `src/components/MobileMenu.tsx` (client component) —
  гамбургер + drawer слева: form-поиск, ссылки каталога, новости/
  сообщения/уведомления с badge, separator, профиль/настройки/справка.
  Scroll-lock body во время открытия, Esc и клик по overlay закрывают,
  auto-close на смену pathname.
- `SiteHeader` на ≤760 px: `.main-nav` и `.search-box` скрываются,
  виден гамбургер + лого (18/28 px) + UserMenu. На ≥761 px drawer
  спрятан через `display:none !important`.

### Фаза 2 (ea71a2d) — главная

- `.novel-grid`: breakpoints 6 (dflt) → 4 (≤1100) → 3 (≤760, gap 14) →
  **2 (≤540, gap 12)**. Раньше 3 колонки тянулись до 700 px, и на
  375-м экране обложка была ~100 px шириной, деформировалась.
- `.story-tile` на ≤540 px: 172×58 → 148×54, title 14→13. Stripe
  нормально влезает.
- `.section-head` на ≤540 px: h2 22→18 px, align-items center (раньше
  baseline → «N ›» вылетала над заголовком).

### Фаза 3 (a0c4250) — страница новеллы + читалка

- `.novel-top` на ≤540 px: grid 1fr, обложка max-width 200 px и
  `margin: 0 auto`, заголовок 34→26 px и центрируется. Раньше на
  мобиле обложка тянулась во всю ширину, занимая пол-экрана высотой.
- `.chapter-item` на ≤540 px: grid `1fr auto` (вместо `1fr auto auto`),
  первая колонка (название+дата) занимает полную первую строку
  (`grid-column: 1 / -1`). Плашка цены и кнопки уходят во вторую
  строку — название больше не обрезается до «Глав…».
- `.reader-main` на ≤540 px: padding 36/22/120 → 20/14/80, заголовок
  26→22.
- `.wallet-block` (TranslatorWallet) на ≤540 px: padding 22/26 → 18/16,
  amount-input 100→80 px, balance 36→28 px.

### Фаза 4 (e77c91e) — переводчик, профиль, admin

- `.translator-hero` на ≤540 px: grid 1fr, центр-текст, avatar
  margin:auto, stats центрируется. Было 72 px avatar + 1fr body на
  360-м — крошилось.
- `.profile-hero` на ≤540 px: flex-direction column, текст центр,
  кнопки Настройки/Админка/Выйти под шапкой justify-content:center +
  flex-wrap:wrap.
- `.bbcode-toolbar` на ≤540 px: `flex-wrap:nowrap` + `overflow-x:auto`
  (свайп) вместо многорядного. Кнопки 30→36 px высотой. Скроллбар
  скрыт.
- `.admin-form-footer`: `padding-bottom: calc(16px + env(safe-area-inset-bottom))` — на iPhone home-indicator больше не перекрывает
  кнопку «Сохранить».

### Фаза 5 (ec0919b) — iOS-полировка

- `.site-header`: `padding-top: env(safe-area-inset-top)`. Липкий
  header остаётся прижат к верху, но внутреннее содержимое сдвигается
  вниз на высоту notch. Без этого логотип лез под динамический остров.
- `.chip`: min-height 36 px + inline-flex + align-items:center. Палец
  попадает уверенно (раньше 28 px высота была на грани).
- `.form-input / .form-textarea`: font-size 14 → **16 px**. Это
  критично: iOS Safari на focus-ах инпутов <16 px авто-zoom-in всей
  странице, после чего layout съезжает. 16 px это убирает.
- `.form-input:-webkit-autofill`: маскируем жёлтый фон автозаполнения
  через inset-shadow цвета `--surface-2`. В тёплой палитре жёлтый
  смотрелся бы грязно.

### Что осталось на будущую итерацию

- **Админ-таблицы** (marketplace-listings, moderation queue, claims)
  на совсем узких экранах могут перерисоваться в карточки —
  сейчас они остаются таблицами с горизонтальным скроллом. Терпимо.
- **ReaderSettings drawer** на iPhone — работает, но анимация
  открытия снизу может наехать на home-indicator. Нужен
  `env(safe-area-inset-bottom)` в его padding.
- **ContinueReadingShelf / MyShelfStrip** — сейчас overflow-x scroll
  с aggressive snap; на iPad может работать странно при landscape
  ориентации. Проверка → фикс, если поймаем жалобу.
- **Landscape-режим телефона** — не тестировал специально; большинство
  правил работает через min/max-width, но возможно некоторые
  sticky-элементы на landscape 320×840 налезут друг на друга.
- Отдельный пользовательский smoke-test на реальных устройствах
  (Алёне и мне) перед анонсом в сторис.

## Итерация 52 — 2-й мобильный проход (жалобы с реального iPhone)

После реального теста на iPhone всплыло, что я прошла только по «каркасу»
страниц. Внутри компонентов осталось ≈20 мест с фиксированными
grid-columns / flex без wrap. Второй аудит через Explore-агента нашёл
18 проблем. Закрыла в 3 батчах.

### Батч 1 — P1 критичные (коммит 4e40069)

- **ReaderSettings drawer** (`/novel/[id]/[chapterNum]` overlay
  настроек) был `width: 360px` без мобильного breakpoint. На iPhone
  SE drawer выходил за экран, кнопки обрезались. Теперь на ≤540 px
  `width: 100%`, heading 18→16 px, `.reader-settings-body` получил
  `padding-bottom: calc(28px + env(safe-area-inset-bottom))` —
  home-indicator больше не перекрывает кнопки. Ещё
  `padding-right: env(safe-area-inset-right)` — iPhone landscape с
  боковым notch.
- **`.icon-btn`** (кнопки в reader-toolbar) 32→44 px на ≤540 px —
  Apple HIG.
- **Каталог `/catalog`** — filter-pills на ≤760 px переведены из
  много-рядного ковра в горизонтальный swipe. Padding боковой,
  scrollbar скрыт. Pill min-height 32→36 px для тап-таргета.
- **`.message-bubble`** в `/messages/[userId]` — `max-width: 70%`
  на узком экране давал полосы по 2 слова. На ≤540 px → 85 %.
- **`.comment-item`** — добавлен `min-width: 0` на родителе + детях
  (длинная ссылка распирала колонку). На ≤360 px (старый SE) avatar
  40→32 px, gap 12→8 px.
- **`.moderation-card`** (`/admin/moderation`) — grid `80px 1fr auto`
  не помещался. На ≤540 px переведён в `72px 1fr` с actions во
  вторую строку (grid-column: 1 / -1).

### Батч 2 — P2 важные (коммит a4a40df)

- **`.friend-row` / `.chat-row` / `.notif-row`** — везде добавлен
  `min-width: 0` на родителе и детях. На ≤540 px padding 14/18→12/14,
  gap 14→10, actions с flex-wrap: wrap, кнопки у друзей — мельче
  шрифт 13 px.
- **`.avatar-preset-grid`** (в AvatarPicker) — на ≤360 px 4×48 не
  влезали, жмём до 3 колонок.
- **`.novels-heat-scroll`** (таблица новелл в `/admin/analytics`) —
  получила `mask-image` градиент справа (правая кромка тускнеет) →
  визуальный хинт «можно свайпнуть вбок». На ≤540 px table padding
  8/10 + `white-space: nowrap` на ячейках (лучше горизонтальный
  скролл, чем рваные цифры).

### Батч 3 — P3 полировка + шапка на мобиле (коммит 0be63ec)

- **`.bookmark-tabs`** (5 вкладок `/bookmarks`) на ≤540 px —
  горизонтальный swipe вместо многорядного wrap.
- **`.subscribers-provider-tabs`** (`/admin/subscribers` tabs
  провайдеров) — тот же паттерн.
- **`.subs-row`** (`/profile/subscriptions`) — `min-width: 0`,
  padding/gap сжат, status-pill мельче.
- **Шапка на мобиле**:
  - `.coin-pill` в UserMenu скрыт на ≤540 px — место в шапке
    ограничено, баланс виден в `/profile` и в drawer'е.
  - `.header-actions > a.btn` скрыт — кнопки «Войти / Регистрация»
    у гостя уже есть в drawer'е.
  - `.header-profile-name` — обёрнут в класс (раньше был голый span),
    на ≤540 px скрыт. Длинный ник больше не пушит dropdown за экран,
    в шапке только аватар + ▾.

### Итого

Покрыты все страницы из списка + компоненты, найденные 2-м аудитом.
Следующий заход — по конкретным жалобам с реальных устройств.

## Итерация 53 — большой список мелких правок от Алёны (7 батчей)

Работа без Алёны, по её списку задач.

### Batch 1 (`82679f2`) — чистка главной

- **Сториз удалены** с главной и из приложения (Алёна: «стереть вообще»).
  page.tsx, компонент StoriesStrip.tsx, ~150 строк CSS выпилены.
- **«О чём сейчас говорят» (CommentsFeed)** переработан: 10→5 комментариев,
  грид-ковер → тонкая лента строк «аватар · автор · новелла · гл. N →».
  Текст комментария не показываем (риск спойлера). CSS полностью переписан.
- **«Забытое на полпути»** — починен отсутствовавший CSS прогресс-бара,
  добавлены min-width:0 + mobile breakpoint (≤540: обложка 72→60, кнопка
  уходит во вторую строку).
- **«Жанры» (GenreChips)** — добавлен CSS для `.chip-count`. Раньше счётчик
  сливался с названием жанра. Теперь pill-бадж 22×20 с accent-wash, white при hover.
- **Avatar picker: не «Telegram», а правильный провайдер.** Пропс
  `telegramPhotoUrl` → `externalPhotoUrl` + `externalProvider`
  ('google'|'telegram'|'yandex'|'other'). Лейблы и hint-тексты подставляются
  по провайдеру из `user.app_metadata.provider`.

### Batch 2 (`edb4f7d`) — multi-translator в форме новеллы

- **Поиск в CreditsEditor переведён с `profiles` на `public_profiles`** (мигр. 040).
  RLS на profiles блокировала всех кроме себя — искать других было невозможно.
- Заголовок «Команда новеллы» → «Переводчики и команда новеллы». Явный упор:
  «можно добавить второго переводчика».
- Быстрые чипы частых ролей: Переводчик / Со-переводчик / Редактор / Корректор /
  Иллюстратор — один тап вместо select. Полный select остаётся ниже.

### Batch 3 (`2ffea19`) — BBCode кнопки в комментариях + дерево

- Новый компонент `CommentToolbar.tsx`: toolbar B/I/U/S/Спойлер/Цитата/Ссылка
  над textarea, выделил текст → обернулся в BB-код. Хоткеи Ctrl+B / Ctrl+I.
  Toggle «Просмотр» — HTML preview через commentToHtml.
- Применён в 3 местах CommentsSection: основной комментарий, reply-форма,
  inline-редактирование. Технические placeholder'ы `[b]жирный[/b]` удалены.
- **Древовидные ответы на любой глубине** — раньше кнопка «Ответить» была
  только для `depth === 0`. Теперь можно ответить на ответ на ответ.

### Batch 5 (`227fc8f`) — шрифты

- UI-шрифт Inter → **Manrope**: вариативный, тёплый, округлый grotesk
  с хорошей кириллицей. Лучше вписывается в «бумажную» палитру.
- Lora для заголовков / читалки остаётся.
- Фолбэк-цепочка `Manrope → Inter → system`.
- `display: swap` — не блокируем рендер.
- FONT_OPTIONS читалки: лейбл ключа 'sans' обновлён на «Manrope».

### Batch 8 (`7f5ead7`) — «Спасибо переводчику» больше не съезжает

Корень: лейбл кнопки «♥ Спасибо» ↔ «✓ Поблагодарил(а). Нажми, чтобы снять»
менял ширину ~3× → весь flex-row дёргался.
Фикс: симметричные лейблы «♥ Спасибо» / «✓ Спасибо», расшифровка ушла
в title. `.chapter-thanks-btn` с `min-width: 130px` для подстраховки.

### Batch 7 (`25586be`) — несколько обложек

- **Мигр. 046**: `novels.covers jsonb` — массив доп. обложек. Главная
  остаётся в `cover_url` (tene.fun продолжает). novels_view пересоздан
  с колонкой covers.
- Админ-компонент `ExtraCoversUpload` — grid-галерея tile'ов 3:4:
  drag-n-drop + multi-file, кнопки «✕ удалить», «← переместить влево»,
  счётчик «#N», tile «+» для добавления. Max 9.
- NovelForm: поле `values.covers: string[]` в форме и payload.
- Edit page: initial.covers из novel.covers при загрузке.
- Новый клиентский компонент `NovelCoverCarousel` для NovelCard: touch
  swipe на мобиле, боковые стрелки на hover десктопа, dot-индикатор
  снизу (активный dot — pill 16×). Одна обложка — карусель не
  рендерится, работает старый `<img>`.
- NovelCard пропс `extraCovers?: string[]`; собирает `allCovers =
  [cover, ...extras]`. Если длина >1 — карусель.
- /catalog подтягивает `novels.covers` и прокидывает.

### Batch 6 (`9c3f92c` в repo server) — бот: массовые операции

Три новые команды `@chaptifybot` для переводчиков с 1000+ подписчиков:
- **`/pending`** — выгружает до 20 pending-заявок отдельными сообщениями
  с inline-кнопками «✓ Одобрить / ✗ Отклонить». Ники, суммы, коммент —
  всё в тексте. Пакетная работа без захода на /admin/subscribers.
- **`/subs`** — счётчик активных + список истекающих в ближайшие 7 дней
  (первые 10 с датой). Напоминалка «кому пора напомнить».
- **`/revoke C-XXXX [причина]`** — досрочно отозвать подписку. Находит
  active subscription по (user_id, translator_id, provider) и вызывает
  RPC `revoke_subscription` (мигр. 041). Читатель получает push.
- HELP_TEXT обновлён — в секции «Для переводчиков» теперь полный набор.

### Не сделано в этой итерации (на следующую)

- **Batch 4 — читалка** (оглавление, переключение «страницы vs свиток»,
  полная тёмная тема сайта). Это крупная работа, требующая глубокого
  прочтения tene `ChapterReader.jsx` и нового дизайна темы.
  Разнесу отдельной итерацией 54, чтобы не смешивать с мелкими фиксами.

## Итерация 54 — апгрейд читалки + глобальная тёмная тема

### TOC drawer (`9e7919c`)

- Новый компонент `src/components/reader/ChapterTOC.tsx`. Портал в
  `document.body`, z-index 9999 — перекрывает sticky-header.
- Кнопка «≡ Оглавление» в reader-toolbar. Загружает все опубликованные
  главы новеллы за один запрос, подсвечивает текущую, показывает бейдж
  с ценой у платных.
- Esc / клик по overlay / клик по главе закрывают. Body scroll-lock
  на iOS через `position: fixed; top: -scrollY` (тот же паттерн что
  в MobileMenu).
- Safe-area top/right/bottom — в dropdown не залезает ни notch, ни
  home-indicator.

### Глобальная тёмная тема приложения (`baf3764`)

Корень жалобы «тёмная тема — откровенное дерьмо»: `reader-wrapper[data-theme="dark"]` переопределяла переменные только внутри читалки.
Шапка, футер и страницы вокруг оставались светлыми — ночью в лицо.

- `html[data-theme="dark"]` теперь переопределяет ВСЕ основные
  CSS-переменные (`--bg`, `--bg-soft`, `--surface`, `--surface-2`,
  `--border`, `--border-strong`, `--ink`, `--ink-soft`, `--ink-mute`,
  `--accent`, `--accent-hover`, `--accent-soft`, `--accent-wash`,
  `--gold`, `--rose`, `--leaf`, `--shadow-sm`, `--shadow-md`).
- Палитра — тёплый графит (#1A1714 / #29241E) + кофейный акцент
  (#D6A06C) + off-white (#EFEADD). Сохраняет «бумажный» характер
  сайта, не плоский чёрный.
- `color-scheme: dark` для адресной строки мобильного браузера.
- `.site-header` background переведён с hardcoded `rgba(245, 239, 230, 0.85)`
  на `color-mix(in srgb, var(--bg) 88%, transparent)` — на тёмной теме
  шапка тоже тёмная.
- Новый компонент `ThemeToggle` — три чипа «☀ Светлая / ☾ Тёмная /
  ◐ Авто». Состояние в localStorage `chaptify-theme`. Режим 'auto'
  слушает `prefers-color-scheme: dark` и авто-переключается.
- Anti-FOUC: inline-`<script>` в `<head>` применяет `data-theme` ДО
  первого paint — иначе страница мигала бы светлой при загрузке в
  dark-режиме.
- Размещение ThemeToggle: в `header-actions` на десктопе (≥761 px)
  и в `mobile-drawer` на мобиле.

Reader dark-тема остаётся независимо — можно сайт оставить светлым,
а читалку переключить на dark (например, ночное чтение на светлом
сайте).

### Режим чтения «Страницы» (`50c73fd`)

Алёна: «переключение со свитка в страницы, как в tene».

- Добавлен `readMode: 'scroll' | 'pages'` в `ReaderSettings` (мигр.
  типа, не БД; localStorage `chaptify-reader-settings`).
- Toggle в ReaderSettings-панели — два чипа «Свиток / Страницы».
- В `ReaderContent` прокидываем `data-read-mode` на `reader-wrapper`.
- CSS `reader-wrapper[data-read-mode='pages'] .novel-content`:
  * `column-width: calc(100vw - 40px); column-gap: 40px;
    column-fill: auto` — 1 колонка на экран.
  * `height: calc(100dvh - 200px)` — 100dvh честнее 100vh на iOS
    (учитывает схлопывание адресной строки).
  * `overflow-x: auto; overflow-y: hidden; scroll-snap-type: x mandatory`
    — горизонтальный свайп с «прилипанием» к странице.
  * `-webkit-overflow-scrolling: touch` + `overscroll-behavior-x: contain`
    для iOS momentum.
  * `break-inside: avoid` на детях — абзац не рвётся между страниц.
- Hint-анимация «»» справа снизу при первом входе в режим — показывает
  что можно свайпать. 2.2s ease-out, один раз, затем исчезает.

### Что не вошло

- **Точное определение номера страницы / X из Y** — требует
  JS-измерений scrollWidth / offsetWidth, отдельного компонента.
  Для MVP достаточно scroll-snap.
- **Восстановление позиции при переключении режима** — сейчас при
  переключении scroll ↔ pages прогресс в главе сбрасывается. В
  следующей итерации если понадобится: через `last_read.paragraphIndex`.
- **Кнопки ‹ / › по бокам для десктопа** — в CSS/JS есть, но
  решила не делать чтобы MVP был проще. Trackpad/mouse-wheel горизонтальный
  скролл работает натурально, клавиатурные стрелки тоже.

## Итерация 55 — полный апгрейд читалки по tene ChapterReader.jsx (5 коммитов)

Алёна: «возьми за основу chapterreader.jsx и переделай читалку, добавляй
весь функционал». Изучила tene-читалку (1385 строк) через explore-агента,
составила приоритизированный план и раскатала 5 фазами.

### Фаза 1.1 (`5f1911a`) — полноценный pages-mode

- State `pageWidth / currentPage / totalPages` в ReaderContent.
- ResizeObserver на `.novel-content` + `document.fonts.ready` →
  пересчёт totalPages = scrollWidth / clientWidth. Триггеры: все
  типографические настройки + readMode.
- Клавиатурная навигация (→/PgDn/Space = next, ←/PgUp = prev,
  Home/End) — только в pages-режиме, не перехватывает input.
- Smart tap-navigation (`onContentClick`): тап по левой трети =
  prev, по правой = next, центр игнорируем (в Фазе 2 будет toggle UI).
  Не срабатывает на button/a/input/glossary-term/quote-bubble.
- Восстановление позиции — в pages-режиме скроллит container
  горизонтально к колонке где находится нужный абзац (через
  offsetLeft / pageWidth).
- UI: кнопки `.reader-page-btn--prev/next` (48×80, видны при hover
  на десктопе, скрыты ≤760 px); индикатор `.reader-page-indicator`
  с текстом «N / Total» и интерактивным range-slider.
- CSS: `column-width: 100%; column-gap: 0; column-fill: auto;
  height: calc(100dvh - 220px); scroll-snap-type: x mandatory;
  -webkit-overflow-scrolling: touch; break-inside: avoid` на детях.
  Скроллбар скрыт.

### Фаза 1.2 (`2f19099`) — mode-aware трекинг абзаца + save

- Раньше `window.scroll` слушался всегда → в pages-режиме (горизонт.
  скролл контейнера) прогресс не трекался.
- Теперь isPages-ветка: `findBestVertical()` по viewport middle или
  `findBestPaged()` — первый абзац с `offsetLeft >= scrollLeft`.
- Слушатель вешается на `container.scroll` в pages / `window.scroll`
  в scroll. Пересоздаётся при смене readMode.
- saveProgress по paragraphIndex — эта метрика стабильна при смене
  шрифта / режима, восстановление работает корректно.

### Фаза 1.3 (`4d1ac0d`) — счётчик комментариев в toolbar

- Кнопка «💬 N» в reader-toolbar рядом с «≡ Оглавление» / «⚙ Настройки».
- Count загружается через `supabase.from('comments').select('id',
  { count: 'exact', head: true })` — без тяги данных.
- Клик скроллит к `.comments-section` внизу страницы. В pages-режиме
  двухэтапно: сначала contrainer.scrollTo(scrollWidth), потом
  sec.scrollIntoView.
- CommentsSection уже внизу страницы (древовидные ответы + BBCode
  toolbar из итерации 53, дополнительной работы не потребовалось).

### Фаза 2 (`e1d76a0`) — visibility-save + prev→last-page + immersive

- **Visibility-save.** На `visibilitychange === hidden` и `pagehide`
  синхронно ищем активный абзац (mode-aware) и сохраняем. iOS/Android
  больше не теряют прогресс при переключении приложения.
- **Prev → end page.** «← Предыдущая глава» в reader-nav передаёт
  `?end=1`. ReaderContent при маунте ловит флаг: scroll → к низу
  body, pages → к scrollWidth контейнера. Логично: пришёл назад
  → начинаешь с конца прочитанного.
- **Immersive UI toggle.** Тап по центральной трети контента в
  pages-режиме скрывает reader-toolbar и индикатор через `ui-hidden`
  класс. Второй тап возвращает. CSS transition 180 мс.

### Фаза 3 (`35a3f6a`) — похожие новеллы в конце главы

- `SimilarByReaders` рендерится после `CommentsSection` на chapter-page.
- Данные через RPC `get_similar_novels_by_readers(novel_id, 6)` (мигр. 003).
- translator_slugs для кликабельных имён — через
  fetchTranslatorSlugs (public_profiles, мигр. 040).
- Если RPC не накачена / список пуст — блок не отображается.

### Что НЕ перенесено (в очередь на будущее)

Из исходного аудита tene (20 пунктов) осталось 5 P2/P3:
- Редактирование главы прямо из читалки (admin-feature) — это правильнее
  делать из админ-панели.
- Prefetch следующей главы (контента из Storage) — Next.js Link уже
  префетчит SSR-страницу, этого достаточно.
- Кастомный toast в читалке — сейчас хватает alert / console для
  редких ошибок.
- Auto-hide UI на последней странице — конфликтует с ручным toggle
  из Фазы 2; делать позже если будет нужно.
- Markdown в комментариях — BBCode уже покрывает нужные форматы.
